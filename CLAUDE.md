@AGENTS.md

## 작업 방식·제약 (모든 세션 공통)
새 세션은 **`docs/세션-온보딩.md`를 읽고** 그 작업방식·커밋규율·제약·병렬세션(worktree) 셋업을 따른다. (커밋은 만진 파일만 `git add`, 배포=`git push`, tsc+build 통과 후, API키 대신입력 금지 등)

## ✍️ 우리가 쓰는 글의 「AI 티」는 «장치»에서 난다 (09-07 대표 지적)

적용 대상 = **채팅 답변 · 보고 · 아티팩트 · 볼트 노트.**
(소개서·브리프 문안은 별개 층이고, 그 정본은 볼트 [[브랜드-보이스]]다.)

대표가 아티팩트를 보고 「AI 티 난다」고 했는데 정량 계측기는 「깨끗하다」고 했다.
우리 글엔 번역투·피동이 원래 0건이라서다. 진단해 보니 티는 전부 **내가 강조하려고 넣는 장치**에 있었다.

1. **강조부호(«» 「」 ‘’)는 한 문단에 한 쌍까지.** 남의 말 인용과 그 문단의 결론에만 쓴다. 나머지는 부호만 벗긴다(내용 변경 아님).
2. **대시(—)로 문장을 잇지 않는다.** 마침표로 끊거나 연결어미로 잇는다. 라벨(`훔칠 것`)에만 남긴다.
3. **「A가 아니라 B」는 한 문서에 둘까지.** 대조가 곧 정보인 곳만 남기고 나머지는 평서로 푼다.
4. **섹션마다 같은 틀을 찍지 않는다.** 같은 2단 구성이나 같은 첫 문장 형태가 셋 넘게 반복되면 템플릿으로 읽힌다.

⭕정답 문형: `광고는 보는 사람이 있어야 팔려요. 우리는 사장님 풀은 만들 수 있어도 방문자 풀이 없어요.`
⛔같은 뜻을 부호와 대시로 감싼 나쁜 예는 `scripts/prose/count-tells.py` 머리말에 있다.

🤖**발행 전엔 읽지 말고 «센다»**: `python3 scripts/prose/count-tells.py <파일>` (LLM 콜 0회).
아티팩트는 이 검사가 **훅으로 자동 실행**된다(`.claude/hooks/artifact-prose-gate.py`).
🚦**막는 건 «강조부호» 하나뿐이다.** 대시·대구는 숫자만 보이고 판단은 사람이 한다.
라벨과 헤딩은 세지 않는다(규칙 2가 이미 열어 둔 자리다). 표본 6종에 돌려서 정했고
**소개서 확정본 46,234자와 브리프 확정본이 통과하는 선**이다. 못 가르는 지표 넷은 그 대조로 버렸다.
이유는 전부 스크립트 머리말에 숫자와 함께 있다.

📌더 길거나 밖으로 나가는 문서(소개서 문안·브리프·노션 발행물)는 `/humanize-korean` 스킬로 진단부터 윤문까지 돌린다.
🚨**단 윤문 콜에 「우리 확정 형식」을 보존 지침으로 «먼저» 넘겨라.** 그 스킬은 남이 만든 것이라 우리 형식을 모른다.
2팀 실측(09-07): 고요 브리프에서 **지운 열 자리 중 다섯이 확정 형식**이었다(`✨` 관찰 문장 · `⚠️` 못 본 것 ·
`[참고]` 블록 · 첫 줄 안내문 · 응원 문형). 진단은 맞았고(장식이 뒤에 몰림·면책 반복) **처방이 우리 것을 지웠다.**
**규칙 4(같은 틀 반복)는 계기판이 못 잡는다. 진단 콜이 잡는다** — 2팀 실측(09-07): 고요 브리프 초안은
절마다 `제목 → 결론 → 표 → 소제목` 한 틀이었는데, 진단이 그 층을 지목하고 윤문이 소제목 넷 중 셋을
지우면서 여섯 절이 다 달라졌다. 계기판을 통과해도 이 병은 남는다.

### 📇 아티팩트·카드형 문서 — 진짜 티는 「같은 칸끼리」에 있다 (09-10 실측)

계기판은 부호만 센다. 그런데 **아티팩트가 AI 티 나는 자리는 거기가 아니라 «반복 구조 안»**이다.
09-10 아티팩트(카드 13장)를 진단에 걸었더니 **계기판은 통과했는데** 이게 나왔다 —
「다시 오는 이유」 13칸 중 **6개가 「~할 때마다」 한 금형** · 평가 13칸이 **전부 2문장 박자** ·
「제일·유일하게」 **8자리** · 한 줄 요약 13개가 **전부 20~30자 단문**.
09-07 「풀 위의 장사 지도」도 같은 병이었다(조각 여섯이 전부 「진단 → 훔칠 것」 2단 틀).

⭐⭐**발행 전에 «세로로» 한 번 훑어라.** 카드를 가로로 읽지 말고 **같은 칸만 세로로 이어 읽는다.**
13개 `ret`만 쭉, 13개 `note`만 쭉. 형태가 붙어 있으면 그 자리가 티다. **0콜이고 30초면 된다.**

1. **같은 칸이 셋 넘게 반복되면 형태를 벌린다.** 종결·길이·짜임 중 **둘은 달라야** 한다.
   ⛔전부 「~할 때마다」로 끝남 / ⛔전부 2문장 / ⛔전부 20~30자
2. **최상급 부사는 한 문서에 둘까지.** 「제일·가장·유일하게·특히」. 남길 곳은 **숫자 근거가 붙는 자리**.
3. **분열문(「~ㄴ 건 X입니다」)은 셋까지.** 「값진 건 첫해입니다」·「찾은 건 성장 리듬입니다」가 잇달으면 하나만.
4. **결산 대구는 문단당 하나.** 「죽고 산다」 같은 쌍이 한 문단에 두 벌이면 뺀다.
5. **평가 칸의 문장 수를 섞는다.** 1문장·2문장·3문장이 골고루 오게.

🚦**계기판 통과 ≠ 깨끗함.** 계기판은 부호·대시·대구만 센다. **위 다섯은 눈으로 본다.**
💸그리고 이걸 지키면 스킬을 안 불러도 된다 — `/humanize-korean` 한 번이 **약 20만 토큰·6분**이다(09-10 실측:
진단 94,000 + 윤문 109,043). 참고 자료를 통째로 읽는 고정비라 **글이 짧아도 같은 값**이 나온다.

## Brain (Obsidian Vault — PARA)
세션 시작 시 반드시 먼저 실행:
```
Read /Users/youngduck/Desktop/collab5-Obsidian/INDEX.md
```

INDEX 확인 후:
- `📢 알림` → FYI 숙지 (액션 불필요)
- `⚠️ 미읽 멘션` → 해당 노트 로드 후 처리

### Vault 구조 (PARA)
| 폴더 | 용도 | 예시 |
|---|---|---|
| `1_Projects/` | 지금 끝낼 것 (완료조건 있음) | 브랜드-소개서, 위저드-모바일-QA |
| `2_Areas/` | 꾸준히 유지하는 기준 | 디자인-시스템, 의사결정-로그, 인프라-운영 |
| `3_Resources/` | 언젠가 쓸 참고 | PARA-운영법, API 문서 |
| `4_Archive/` | 완료되어 치운 것 | 완료-마일스톤 |
| 🆕`5_Backlog/` | **안 하고 있는 것** — ⭐`백로그-보드.md` **하나만 본다** | 콜라보-요청-생명주기, /card 재활용 |

### 저장 규칙
- 새 작업 → `1_Projects/` 노트 (완료조건 명시)
- 결정·정책·왜 이렇게 했나 → `2_Areas/의사결정-로그`
- 디자인 스펙 변경 → `2_Areas/디자인-시스템`
- 작업 완료 → `4_Archive/`로 이동 + INDEX 내림
- 🆕**백로그(미루는 일) → `5_Backlog/백로그-보드.md`에 한 줄** (08-06 대표 지시). ⭐**"백로그 보자" = 그 파일 하나만 연다** — 다른 노트에 `⏳백로그`를 흩뿌리지 말 것. 착수하면 보드에서 지우고 `1_Projects`로 승격

### 팀 커뮤니케이션
- **hook** ("~팀에 알려줘") → INDEX `📢 알림`에 FYI 추가
- **mention** ("~팀에 멘션/요청") → 대상 노트 `⚠️ 미읽 멘션`에 체크박스 추가 + INDEX 업데이트

Vault 경로: `/Users/youngduck/Desktop/collab5-Obsidian/`
