"use client";

// 홈 상단 메뉴바 — 헤더 바로 밑에 붙어 페이지 끝까지 따라오는 **2칸** 바(대표 지시 08-14, 레퍼런스 heytaby.com).
// 📜칸 수 이력: 3칸(매거진 포함) → 2칸(08-16) → 3칸(소개서 등록 추가) → **2칸(08-17)**.
//   ⭐**칸 수와 라벨 길이는 한 쌍**이다 — 이 파일에서 세 번 확인된 규칙이다. 칸을 늘리면 라벨을 깎게 되고,
//     줄이면 말을 되찾는다. 라벨을 건드릴 땐 canvas measureText로 먼저 재라(아래 실측 기록 참조).
//
// 이 바가 푸는 문제는 하나다: **모바일에서 매거진·콜라보 찾기로 들어갈 길이 없었다.**
// 헤더의 「매거진」은 `hidden sm:flex`(데스크톱 전용 — 좁은 폰에서 가로 스크롤이 터졌던 자리)라
// 모바일 진입은 사실상 풋터뿐이었다. 그래서 홈에서는 그 두 링크를 **헤더에서 내려 이 바로 옮긴다**
// (헤더는 홈에서 로고+계정만 남는다 — `HeaderNavLinks`가 경로로 판정).
//
// ⚠️ **활성 표시(스크롤스파이·선택된 알약)를 일부러 안 쓴다.**
//    heytaby의 탭바는 네 칸이 전부 한 페이지 안의 앵커라 볼드가 「지금 여기」를 뜻한다. 여기는 **두 칸 중
//    하나가 페이지를 떠나는 링크**(`/search`)다 — 같은 생김새에 다른 뜻을 담으면 활성 표시가 무너진다.
//    🔁08-17에 대표가 *"앵커로 이동하면 그 칸을 눌린 상태로 유지(포커싱)"*를 검토했다가 **안 하기로 확정.**
//      2칸이 되면서 문제가 오히려 선명해졌다 — 앵커는 한 칸뿐이라 **영원히 안 켜지는 칸이 생긴다.**
//      「왜 저쪽만 되지?」로 읽히느니 둘 다 안 켜지는 편이 낫다.
//    생김새(둥근 트랙 + 알약)만 빌리고 「지금 여기」 문법은 안 빌린 이유다.
//    ※ 섹션 ③의 `HomeSectionTabs`는 반대다 — 거긴 두 칸 다 앵커라 스크롤스파이가 정직하다.
//
// ⚠️ 왜 클라 컴포넌트인가 — 앵커 점프에 JS가 필요해서다(아래 `goAnchor`). 홈은 ISR(revalidate 300)이라
//    **서버에서 세션을 읽으면 홈 전체가 매 요청 렌더로 내려앉는다**(=캐시 증발). 이 파일은 세션을
//    안 읽으므로 홈 HTML은 계속 캐시된 채로 있다 — 그 상태를 깨지 마라.
//    🔻~~세 번째 칸을 소개서 유무로 갈라 라벨을 바꾸던 로직~~은 08-16에 삭제됐고, 그 칸 자체가 08-17에 없어졌다.
import Link from "next/link";
import { track } from "@/lib/track";
import { IDEA_CTA_ANCHOR } from "./HomeIdeaCta";

// 두 칸 공통. 활성/비활성 구분이 없으므로 상태 클래스도 없다(위 ⚠️ 포커싱 항목 참조).
// 🔤 크기 — 대표 지적 08-14 *"메뉴 치고 폰트가 너무 작다"*. 13/14 → **양쪽 다 15**(디자인팀 확정).
//    ⚠️처음엔 모바일 15 / 데스크톱 15.5로 갈랐다가 되돌렸다. **1px 안팎의 차이는 위계로 안 읽히고
//      실수로 읽힌다**(디자인팀). 헤더 nav는 14로 두는데, 그 1px 차이가 위계를 만드는 게 아니라
//      **성격이 다른 두 바**(홈 주 내비 / 전역 보조 내비)라는 게 크기로 드러나는 것뿐이다.
//    ⚠️폭은 확인하고 올렸다 — 375px에서 바가 294.6px, 좌우 여유 80.4px.
// 📏 높이 **h-[44px]** — 터치 타깃 권장치를 정확히 맞춘 값이다.
//    🪤`h-10`으로 쓰면 40이 아니라 **42.5px**다. 이 저장소는 루트 폰트가 17px이라 rem 유틸이
//      전부 6.25%씩 크게 나온다(h-9=38.25 / h-10=42.5). 권장 44에 1.5px 모자라서 px로 박았다.
// 🔻08-16 좌우 패딩 `px-3.5`(29.75) → **`px-3`**(25.5), 모바일만. 데스크톱은 `sm:px-5` 그대로.
//    3번칸이 「소개서 등록」(4자) → 「아이디어 추천」(6자)으로 길어지면서 360px에서 알약이 넘쳤다.
//    실측: 라벨만 바꾸면 348.1px(가용 343 초과 ❌) → 패딩 한 단 내리면 335.4px ✅ (여유 7.6px).
//    ⚠️**높이 44는 손대지 않았다.** 터치 타깃은 세로가 정하고, 좁힌 건 글자 옆 여백뿐이다.
//    🪤여유가 7.6px밖에 없다 — 라벨을 또 늘리면 여기부터 다시 재라(canvas measureText로).
const ITEM =
  "flex h-[44px] shrink-0 items-center whitespace-nowrap rounded-pill px-3 text-[15px] font-medium text-mute transition-colors hover:bg-primary-pale hover:text-primary-on sm:px-5";

/** 칸 사이 세로 구분선(대표 제안 08-14 — "메뉴바처럼 보이게").
 *  ⚠️`aria-hidden` + 빈 요소다 — 스크린리더에는 링크 3개만 들려야 한다.
 *  🎨색은 알약 테두리와 **같은 `#DFDFE3`**. 선이 둘(테두리·구분선)인데 색이 다르면 그 자체가
 *     위계로 읽힌다 — 여기선 둘 다 "구획"만 말하므로 같은 값이어야 한다.
 *     ※ C안 이전(회색 트랙일 때)엔 `border-strong`을 썼다. 면이 흰색이 되면서 다시 골랐다 —
 *       **면 위에 선을 얹을 땐 면색이 바뀔 때마다 선색을 다시 판단해야 한다.**
 *  ⚠️두께는 테두리(0.5px)와 달리 **1px**로 둔다. 사각 테두리는 0.5px가 일부만 렌더돼도 나머지
 *     세 변이 형태를 잡아주지만, 홀로 선 세로선은 그 순간 통째로 사라져 보인다.
 *  ⚠️높이는 칸 전체가 아니라 **안쪽으로 인셋**(h-4 = 17px, 알약 44px의 39%). 꽉 채우면
 *     알약의 둥근 모서리와 부딪혀 바가 '표(table)'처럼 읽힌다(디자인팀도 인셋 필수 의견). */
function Divider() {
  return <span aria-hidden="true" className="h-4 w-px shrink-0 bg-[#DFDFE3]" />;
}

/** ⚓3번칸 「소개서 등록」의 점프 목적지 id — `HomeBody`의 ④ 소개서 구좌에 붙는다(08-16).
 *  🔗`IDEA_CTA_ANCHOR`가 `HomeIdeaCta`에 있는 것과 같은 이유로, 값은 **한 곳에만** 둔다.
 *    여기 둔 건 이 상수를 쓰는 쪽(메뉴바)이 여기라서다 — 바꾸면 두 파일이 같이 따라온다. */
export const PREVIEW_ANCHOR = "home-preview";

export function HomeMenuBar() {
  // 🔻08-16 `hasBrand` 조회 **삭제**(3번칸이 소개서 유무로 갈리지 않게 되면서). 그 칸은 08-17에
  //    아예 없어졌다. 방문마다 돌던 세션 쿼리가 하나 줄어 있는 상태다 — 되살리지 마라.

  // 앵커 이동 — 오프셋은 CSS(`scroll-mt-[152px]`)가 정본이라 scrollIntoView가 그대로 존중한다.
  // JS에서 좌표를 다시 계산하면 값이 두 군데로 갈라진다.
  //
  // 🪤**08-16 버그 수정 — `behavior:"smooth"`가 먼 거리에서 죽는다.**
  //    대표 제보 *"콜라보 아이디어 만들기 누르면 앵커가 제대로 동작 안 해"*.
  //    실측: 목표가 3739px 아래일 때 `smooth`는 **scrollY가 0에서 꿈쩍도 안 했고**, 같은 요소에
  //    `auto`를 주면 3579.5px로 정확히 갔다.
  //    ⭐원인 — 홈은 브랜드 카드·리포트 이미지가 lazy 로드되며 **문서 높이가 계속 변한다.**
  //      smooth 스크롤은 애니메이션 도중 레이아웃이 바뀌면 브라우저가 조용히 취소한다.
  //      거리가 멀수록(=시간이 길수록) 그 사이에 이미지가 뜰 확률이 커진다.
  //    👉그래서 **먼 거리는 즉시 점프**한다. 어차피 4천 px을 부드럽게 흘려보내는 건 몇 초짜리라
  //      UX로도 나쁘다 — 짧은 거리에서만 smooth가 의미 있다.
  const goAnchor = (id: string, event: string) => (e: React.MouseEvent<HTMLAnchorElement>) => {
    const el = document.getElementById(id);
    if (!el) return; // 못 찾으면 네이티브 해시 점프에 맡긴다
    e.preventDefault();
    track(event);
    const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    // 화면 두 개 분량(2×innerHeight)을 넘으면 smooth를 포기한다. 그 이하면 부드럽게.
    const far = Math.abs(el.getBoundingClientRect().top) > window.innerHeight * 2;
    // 🎯`block:"start"` — 목적지가 **섹션 제목**이라(08-16 대표 지시로 CTA 버튼에서 옮김)
    //    위에 붙여야 제목부터 읽힌다. 오프셋은 그 섹션의 `scroll-mt-[152px]`가 알아서 준다.
    //    ⚠️`"center"`로 두면 제목이 화면 한가운데 떠서 위쪽에 이전 섹션 꼬리가 크게 남는다.
    el.scrollIntoView({ behavior: reduce || far ? "auto" : "smooth", block: "start" });
  };
  const goIdea = goAnchor(IDEA_CTA_ANCHOR, "home_menubar_idea_click");

  return (
    // 🎈**플로팅**(대표 확정 08-14) — 알약만 콘텐츠 **위에** 뜨고, 알약이 덮지 않는 좌우로는
    //    본문이 그대로 보인다. heytaby도 이 방식이다(그쪽 캡처에서 헤드라인이 알약 양옆으로 보인다).
    //    🔻처음엔 캔버스색 풀블리드 밴드를 깔았다(box-shadow+clip-path). 그건 `HomeSectionTabs`가
    //      08-02에 당한 버그 — "알약 폭이 화면보다 좁아 제목이 좌우로 삐져나온다" — 의 처방인데,
    //      **대표는 그 삐져나옴을 버그가 아니라 원하는 그림으로 봤다.** 밴드가 본문을 가리는 쪽이
    //      오히려 손해라는 판단이라 걷어냈다.
    // 🚨 `pointer-events-none` 필수 — 이 래퍼는 화면 폭 전체를 차지하는 투명 띠다. 그냥 두면
    //    **알약 좌우의 빈 곳이 뒤 콘텐츠의 클릭을 통째로 먹는다**(보이지도 않는 것이 막으니 원인을 못 찾는다).
    //    알약에만 `pointer-events-auto`를 되돌려준다.
    // z-[6] = 헤더(z-10) 아래, HomeSectionTabs(z-[5]) 위.
    // top-14 = 헤더 높이. ⚠️루트 폰트가 17px이라 실제 59.5px다(56 아님 — 눈대중 금지).
    // 🛟`px-2` + 아래 nav의 `max-w-full overflow-x-auto` = **가로 스크롤 안전장치**.
    //    라벨이 「콜라보 매거진」으로 길어지며 375px 기준 알약이 336.2px가 됐다(좌우 여유 19.4씩).
    //    375·360은 들어가지만 **320px(구형 SE)에서는 알약이 화면을 넘겨 페이지 전체에 가로 스크롤이
    //    생긴다** — 이 저장소가 07-29에 헤더로 한 번 당한 그 사고다.
    //    → 넘칠 땐 **알약 안에서만** 스크롤되게 가둔다. 지면이 밀리는 것보다 훨씬 나은 실패 모양이다.
    //    ⚠️`px-2`(8.5씩)는 360px에서 여유가 남도록(343 > 336.2) 고른 값이다. 더 키우면 360에서도
    //      불필요하게 스크롤 레일이 생긴다 — 라벨을 또 늘릴 땐 이 숫자부터 다시 재라.
    <div className="pointer-events-none sticky top-14 z-[6] flex justify-center px-2 py-2">
      <nav
        aria-label="홈 바로가기"
        // 🎨C안(대표 확정 08-14) — **흰 면 + 0.5px 라인 테두리**.
        //   A안(회색 트랙)은 순백 페이지에 회색 덩어리가 하나 뜨는 게 걸렸고, B안(면·선 전부 없음)은
        //   "떠 있는 바"라는 이 개편의 출발점을 잃었다. C는 둘의 가운데 — 면은 지면과 같은 흰색이라
        //   덩어리로 안 읽히고, 라인이 "여기까지가 메뉴" 구획만 말한다.
        // 🔒`border-[0.5px] border-[#DFDFE3]`는 **브랜드 카드·매거진 히어로와 완전히 같은 값**이다
        //   (대표 지시 "브랜드 카드 그거로 참고"). 같은 사이트의 같은 뜻이면 같은 선을 쓴다 —
        //   여기서 값을 새로 만들면 라인 어휘가 셋으로 갈린다.
        // 📐그림자 **e1 → e2**(08-14). e1은 `0 1px 2px 6%`로 네 단계 중 제일 낮은 값이다 —
        //   **떠 있어야 할 것에 가장 안 뜨는 그림자**를 쓰고 있었다. 지면을 낮춘 것과 한 쌍으로 올린다.
        //   ⚠️e3(`0 8px 32px 14%`)까지 가면 알약이 모달처럼 무거워진다. 메뉴는 지면 바로 위 한 층이다.
        className="no-scrollbar pointer-events-auto inline-flex max-w-full items-center gap-1 overflow-x-auto rounded-pill border-[0.5px] border-[#DFDFE3] bg-surface p-1 shadow-e2"
      >
        {/* 🔻08-16 「콜라보 매거진」칸 **삭제** — 대표: *"플로팅바가 너무 길다. 매거진은 헤더 상단바로
              다시 올리고, 여기는 콜라보 찾기 | 콜라보 아이디어 2개로 가자"*.
            ⭐3칸 → 2칸이 되면서 라벨 길이 압박이 통째로 사라졌다. 그래서 아래 3번칸 라벨도
              「아이디어 추천」에서 **대표가 원래 말한 「콜라보 아이디어 만들기」로 되돌렸다**(실측 재확인).
            🚨매거진은 이제 헤더(`HeaderNavLinks`)로 갔는데 그건 `hidden sm:flex`라 **폰에선 안 보인다.**
              → 홈 모바일의 매거진 진입로는 **최상단 매거진 배너**가 맡는다. 배너를 지우면 폰에서
                매거진에 닿는 길이 풋터 하나만 남는다(둘 중 하나는 반드시 있어야 한다). */}
        <Link href="/search" onClick={() => track("home_menubar_search_click")} className={ITEM}>
          콜라보 검색
        </Link>
        <Divider />
        {/* 🔻08-16 대표 지시 — 「소개서 등록」/「콜라보 추천」 분기를 **없애고 한 칸으로 합쳤다.**
            두 사람 모두 홈 ③구좌의 「콜라보 아이디어 추천 받기」 버튼으로 내려간다.
            ⭐그 버튼이 이미 3분기(비로그인 / 소개서 없음 / 있음)를 처리하기 때문이다.
              판정이 두 군데 있으면 **메뉴는 "소개서 등록"이라 하고 버튼은 "로그인하세요"라 하는 식으로
              어긋난다.** 판정은 한 곳에서만 한다.
            ⚠️`<button>`이 아니라 `<a href="#id">`인 이유: JS가 죽어도 네이티브 해시 점프가 대신
              동작하고 키보드·새 탭·링크 복사를 공짜로 얻는다.
            🪤라벨 폭 — 3칸일 땐 「콜라보 아이디어 만들기」가 360px에서 403.6px로 넘쳐서(가용 343)
              「아이디어 추천」으로 줄였었다. **매거진 칸이 빠지면서 여유가 생겨 원래 말로 되돌렸다.**
              라벨을 또 바꿀 땐 canvas measureText로 다시 잴 것 — 2칸이어도 무한하진 않다.
            ✏️08-16 2차 — 「만들기」 → **「찾기」**(대표 지시). 한 글자 짧아지므로 폭은 안전한 방향이다.
              ⭐뜻도 이쪽이 맞다: 이 칸이 하는 일은 **아이디어를 만드는 게 아니라 받아보는 것**이고,
                목적지(③구좌 버튼)도 「나도, 콜라보 아이디어 추천받기」다. 「만들기」는 소개서 작성과
                헷갈릴 여지가 있었다. 1번칸 「콜라보 찾기」와 어미가 맞아 두 칸이 한 쌍으로 읽힌다. */}
        {/* ✏️08-17 대표 지시 — 「아이디어 추천」 → **「콜라보 아이디어 분석」**.
            ⭐3번칸이 빠지면서 폭 압박이 통째로 사라져 말을 다시 늘릴 수 있게 됐다. 「분석」이
              맞는 말인 이유 = 목적지(③구좌)가 하는 일이 **내 소개서와 상대 소개서를 함께 읽고
              분석하는 것**이고, 그 버튼 아래 부제가 정확히 그 문장이다.
            ⭐병렬도 오히려 좋아진다 — 두 칸이 **둘 다 「콜라보」로 시작**해
              `[콜라보][검색]` / `[콜라보 아이디어][분석]`로 읽힌다.
            📏실측(canvas measureText, 15px/500 Pretendard 기준 · 이 파일의 정본 방법):
              · 현행 3칸                      322.4 → 360 +20.6 / **320 −19.4 ❌**
              · **2칸 「콜라보 아이디어 분석」  261.5 → 360 +81.5 / 320 +41.5 ✅** ← 지금 이것
              · 2칸 「콜라보 분석」            206.0 → 320 +97.0 (더 줄이고 싶을 때의 여분)
              ⭐**320px에서도 처음으로 안 넘친다.** 3칸 시절엔 구형 SE에서 알약 안 스크롤로
                떨어졌는데, 칸을 줄이니 그 실패 모양 자체가 필요 없어졌다. */}
        {/* ✏️08-31 대표 지시 — 「콜라보 아이디어 분석」 → **「콜라보 그려보기」**.
            ⭐소개서 하단 버튼을 「콜라보 미리 그려보기」로 바꾼 것과 **한 벌**이다(같은 날).
              들어가는 문(홈)과 누르는 자리(소개서)가 같은 말을 써야 같은 일로 읽힌다.
            📏실측 08-31(canvas measureText, 15px/500 Pretendard — 이 파일의 정본 방법):
              ⚠️**아래는 「라벨 글자만」의 폭이다.** 위 08-17 줄의 261.5 같은 값은 «알약 전체»(패딩·구분선
                포함)라 서로 직접 비교하면 안 된다 — 재는 대상이 다르다.
              · 옛 「콜라보 아이디어 분석」 124.0
              · **새 「콜라보 그려보기」     94.4** ← 지금 이것 (**−29.6px**, 말이 짧아져 더 안전해졌다)
              🖥그래서 **알약 전체를 다시 쟀다**: 320px 화면에서 **233px**(여유 87px) · 한 줄 · 가로 스크롤 없음. */}
        <a href={`#${IDEA_CTA_ANCHOR}`} onClick={goIdea} className={ITEM}>
          콜라보 그려보기
        </a>
        {/* 🔻08-17 대표 지시 — 3번칸 **「소개서 등록」 삭제**(2칸으로 복귀).
            ⭐**칸 수와 라벨 길이는 한 쌍**이라는 이 파일의 규칙이 또 한 번 확인됐다. 08-16에
              3칸이 되며 2번칸을 「콜라보 아이디어 찾기」→「아이디어 추천」으로 깎았는데,
              칸을 도로 줄이니 말을 **원래보다 더 길게**(「콜라보 아이디어 분석」) 쓸 수 있게 됐다.
            🔗**④ 소개서 구좌로 가는 앵커가 이 메뉴에서 사라진다.** 그 자리는 플로팅 알약
              (「3분 만에 소개서 등록하기」)이 맡는다 — 같은 날 ④구좌의 등록 버튼도 지웠고,
              알약이 그 구간 내내 떠 있도록 `data-cta-guard`를 뗐다(HomeBody ④ 주석 참조).
            ⚠️**소개서를 이미 가진 사람에겐 알약이 안 뜬다.** 그 사람은 홈에서 ④구좌로 가는
              길이 없어지는데, 애초에 그 구좌가 답하는 질문(「소개서가 뭔데?」)이 그 사람에겐
              끝난 질문이라 손해가 아니다 — 대표 판단으로 이 상태를 택했다.
            📌`PREVIEW_ANCHOR`(위)는 **남긴다.** ④구좌 `id`와 딥링크(`/#home-preview`)가 계속
              쓰고, 메뉴에 다시 넣을 때 값이 두 곳으로 갈리지 않게 하려는 것도 같은 이유다. */}
      </nav>
    </div>
  );
}
