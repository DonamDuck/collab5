// 활동·콜라보 개수 상한 — **여기만 고치면 된다.**
// spec: docs/superpowers/specs/2026-08-10-activity-collab-30-design.md
//
// 🚨 이 파일이 생긴 이유: 상한 판정이 등록 폼 **네 곳**에 숫자로 흩어져 있었고,
//    그중 하나엔 `⚠️ 상한(5)과 같아야 한다`는 **사람에게 하는 부탁**이 주석으로 달려 있었다.
//    부탁은 지켜지지 않는다 — 실제로 과거에 개수 상한을 한쪽만 고쳐 반대 방향으로 재발한 적이 있다.
//    ⭐새로 상한을 참조하는 자리가 생기면 숫자를 쓰지 말고 반드시 이 상수를 import 할 것.

/** 대표 활동 최대 개수 (2026-08-10, 5 → 30). 무제한으로 열지 않는 이유 = 붙여넣기 폭주를 아무도 못 막는다. */
export const MAX_ACTIVITIES = 30;
/** 함께한 콜라보 최대 개수 (2026-08-10, 5 → 30) */
export const MAX_COLLABS = 30;

/** 소개서에서 처음 보여줄 개수 + [더 보기] 한 번에 늘어나는 개수 (대표 확정 08-09).
 *  30건이면 더보기는 최대 5번. 무한스크롤이 아니라 **명시적 더보기**라 한 번에 그리는 양이 늘 통제된다. */
export const REVEAL_STEP = 5;

/** 인쇄(A4)에 실을 개수. 지류는 요약이다 — 30건이면 몇 장이 된다. */
export const PRINT_ITEM_LIMIT = 7;

/** Brand DNA 입력에 넣을 개수 — **화면 상한과 분리한다**(대표 확정 08-10).
 *  ⚠️분리하지 않으면 화면 상한을 푼 만큼 **AI 입력이 조용히 따라 부푼다**(리포트 원가의 큰 몫이 입력이다).
 *  ✅도입 시점(08-10) 기준 실제 최대가 활동 5·콜라보 5라 7로 잘라도 결과가 같다 →
 *     다이제스트 문자열 동일 → `input_hash` 동일 → **DNA 재생성 0건**. 8건째를 넣는 브랜드부터만 갈린다.
 *  ⏳7이 맞는 숫자인지는 8건 이상 브랜드가 생기면 5·7·10 A/B로 잰다. */
export const DNA_ITEM_LIMIT = 7;

// ── 📰 매거진 (2026-08-10) ──
/** 매거진 사진 최대 변 — 소개서(브랜드 1000·활동 800)보다 크게. 본문 폭이 넓고 표지로도 쓰인다(지시서 §6). */
export const MAGAZINE_IMAGE_MAX_DIM = 1600;
/** Storage 경로 접두사. 버킷은 기존 `maker-photos`를 그대로 쓴다 —
 *  ⛔새 버킷을 만들지 않는다(버킷 생성은 콘솔 작업이라 대표 몫, 지시서 안전규칙 3). */
export const MAGAZINE_STORAGE_PREFIX = "magazine";

/** 매거진 댓글 본문 최대 글자 수 (2026-08-23).
 *  ⚠️**여기 두는 이유가 하나 더 있다**: 이 값을 서버 액션 파일(`magazine-actions.ts`)에 두면
 *    `"use server"` 파일은 **async 함수만 export할 수 있다**는 규칙에 걸려
 *    그 파일의 **모든 export가 통째로 죽는다**(좋아요 액션까지 같이 깨졌다, 08-23).
 *    🚨`tsc`는 이걸 못 잡는다 — 타입이 아니라 Next.js의 규칙이라 빌드/런타임에서야 터진다. */
export const COMMENT_MAX = 500;
