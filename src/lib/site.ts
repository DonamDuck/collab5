// 사이트의 기준 주소 한 곳 — 사이트맵·robots·링크 미리보기가 전부 이걸 쓴다.
// ⚠️ 상수로 박는다(환경변수 아님): 프리뷰 배포마다 주소가 달라지면 사이트맵이 vercel.app 주소를
//    가리키고, 구글은 그걸 **다른 사이트의 목록**으로 보고 통째로 무시한다.
export const SITE_URL = "https://collab5.co.kr";

/** 링크 미리보기(카톡·슬랙·X) 기본 썸네일 — 1200×630. **사진이 없는 페이지는 전부 이걸로 떨어진다.**
 *
 *  ⭐`?v=` 를 붙인 채로 둔다. 카톡·페북 스크래퍼는 이미지를 **URL 단위로** 캐시해서, 파일 내용만
 *    바꾸고 주소를 그대로 두면 **옛 썸네일이 계속 나간다**(08-16 로고 교체 때 실제로 겪음).
 *    그림을 갈 때마다 v를 올릴 것.
 *
 *  🪤 카톡은 이미지뿐 아니라 **페이지 스크랩 결과도** 따로 캐시한다. v를 올려도 이미 퍼진 링크는
 *     그대로일 수 있다 — 급하면 카카오 개발자 도구에서 주소별로 초기화해야 한다
 *     (https://developers.kakao.com/tool/clear/og).
 */
export const OG_IMAGE = "/og-image.png?v=2";
