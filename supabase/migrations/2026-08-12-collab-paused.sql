-- 콜라보 요청 잠시 안받기 (2026-08-12, 대표 지시 — 계단뿌셔클럽 사장님 피드백)
--
-- 배경: 07-31에 「콜라보 받는 중」 토글(`collab_open`)을 지우면서 근거가
--       *"[검색 노출]이 곧 콜라보 가능"* 이었다. 그런데 08-07에 그 전제를 우리 손으로 깼다
--       — `search_visible`을 「콜라보 찾기에 보이기」(사이트 안 목록)로 좁히고 웹 검색과 분리했다.
--       그래서 "요청을 잠시 안 받는다"를 말할 자리가 사라진 상태였다.
--
-- ⚠️`collab_open`을 되살리지 않고 새 컬럼을 판 이유:
--   ① 이름이 반대 방향이라(open=받는 중) 지금 UI(「잠시 안받기」)와 계속 어긋난다.
--   ② 옛 값은 6주 전 것이고 그 사이 의미가 바뀌었다 — 되살리면 그때 꺼둔 사장님이
--      영문도 모르고 "요청 잠시 안 받는 중"이 된다.
--
-- ⭐확장 방향이라 **DB 먼저, 코드 나중**이 맞다(축소는 반대). 이 SQL을 먼저 돌리고 배포한다.
--   default false = 기존 소개서는 전부 "받는 중" — 아무도 갑자기 잠기지 않는다.

alter table brands
  add column if not exists collab_paused boolean not null default false;

comment on column brands.collab_paused is
  '콜라보 요청 잠시 안받기. true면 /m에서 [콜라보 제안 시작하기]가 잠기고 타이틀 옆에 안내 칩이 뜬다. 검색·목록 노출(search_visible)과는 별개다.';
