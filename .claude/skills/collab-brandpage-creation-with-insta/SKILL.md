---
name: collab-brandpage-creation-with-insta
description: 컨시어지 소개서 제작 v3 — **위저드를 돌리지 않고 리서치 결과를 폼에 직접 넣는다**(유료 0콜, 대표 확정 08-21). 대표가 상호·하는일·인스타(·홈페이지·블로그·브런치)를 주면 ①전량 수확해 브랜드 브리프를 만들고 ②후보 아티팩트로 대표 판정을 받고 ③폼에 직접 입력해 발행하고 ④사진 회수·선별·이관까지 한 흐름으로 잇는다. "○○ 소개서 만들어줘(인스타까지)", "리서치부터 하고 소개서", "풀코스 제작", "컨시어지 제작" 요청 시 사용. (08-05 대표 설계 · 08-21 파랑~ 님 건에서 완주 검증)
---

# collab-brandpage-creation-with-insta — 리서치 먼저, 생성은 그다음

> **한 줄**: 인스타·블로그·브런치를 **전량 먼저 읽고**, 그 결과를 **폼에 직접 넣는다.**
> ⭐**위저드는 돌리지 않는다**(08-21 확정) — 위저드는 우리 수확본을 못 읽어서 더 얕은 결과에 돈만 든다.
> 이 스킬은 오케스트레이터다 — 세부 기법의 정본은 [[collab-brandpage-creation-basic]]·[[collab-page-enrich]] 해당 절.
> ✅**08-21 파랑~ 님(지인 아닌 첫 고객) 건에서 리서치→발행까지 완주**했다. 그때 나온 실전은 §②에.

## 왜 이 순서인가 (2026-08-05 대표 설계 + 1팀 검증)

1. **칩 선택에 근거가 생긴다.** 구 모드 A의 칩 기준은 "넓게" 한 단어뿐이었다(사람 감).
   브리프가 먼저 있으면 **근거 있는 것만** 고르고, 크롤이 놓친 건 "직접 추가"로 넣는다.
2. **사진 1장 필수(08-05 배포)와 정합.** 신규 등록은 이제 사진 없이 저장이 안 된다 —
   구 모드 A(프리필→바로 저장)는 **그대로는 깨진다.** 리서치가 먼저면 저장 시점에 사진이 이미 폴더에 있다.
3. **리서치 한 번을 끝까지 쓴다.** 브리프 → 후보 아티팩트 → 문안 → 사진 선별이 **같은 수확물** 하나를 계속 쓴다.

⚠️ **위저드의 AI 크롤은 우리 리서치를 못 읽는다** — 이것이 08-21에 위저드를 폐기한 이유다.
그전에는 「사람이 만지는 지점(칩·별표·게이트)에서 회수한다」였는데, **폼에 직접 넣으면 회수할 것도 없다.**
위저드는 [[collab-brandpage-creation-basic]] 모드 A(하한 측정)로만 남는다.

## 전체 지도

```
⓪ 입력·동의 확인 → ① 사전 리서치(브리프) → ② 폼 직접 입력(⭐위저드 안 씀·유료 0콜)
→ ③ 후보 아티팩트 → 대표 판정 → ④ 문안 확정 + 저장 → ⑤ 사진 회수 → ⑥ 대표 선별·발행·이관
```

대표 개입 지점 = **4곳**: ⓪입력 · ③후보 판정 · ⑤사진 선별 · ⑥발행 확인.
**유료 콜 = 0.** 리서치도 폼 입력도 전부 무료(브라우저·HTTP)다.

⚠️**2026-08-21 이전 판에는 「② 위저드 생성(유료 1콜)」이 있었다.** 폐기한 이유는 §②에.

---

## ⓪ 입력 + 동의 확인

대표가 준다: **상호 · 주소(지역) · 하는 일(업종) · 홈페이지 URL · 인스타 핸들.**
(주소·업종은 위저드 씨앗의 필수 입력이자 동명 업체 방어 — 없으면 여기서 되물어라.)

### 🔗🚨 그 전에 — **링크 허브부터 연다** (08-25 터 건, 세 번째 같은 실수)

인스타 핸들을 받으면 **바이오의 링크(리틀리·링크트리·인포크)를 «맨 먼저» 연다.**
거기엔 **그 사람이 글을 쓰는 채널 목록이 통째로** 있다. 안 열면 **인스타만 세고 「직접 쓰신 글」이라 부르게 된다.**

🩸**08-25 실측 — 그 차이가 두 배가 넘었다.**
| | 리틀리를 열기 전 | 열고 나서 |
|---|---|---|
| 「직접 쓰신 글」 | 인스타 **132편** | 인스타 132 + **네이버 블로그 81 + 브런치 63 = 276편** |
| 「배우러 다니신다」 근거 | 3건 | **+ 블로그 수강 노트 20편**(새벽 해부학 18강 정리) |
| 책 | 프로필 카드에서 4권 | **스마트스토어 링크 4종 + 굿즈**(「미니잡지 20권 한정」까지) |

⭐**그리고 인스타에 없는 «시기»가 통째로 나온다** — 블로그는 **2022-12**부터였는데 인스타 수확은 2025-07이 하한이었다.
「활동 기간 3년 9개월」을 「13개월」로 적을 뻔했다.

⚠️**이건 §④-A의 「내 수확 범위가 그 시기를 덮는가」와 «같은 병의 상류»다.**
범위를 확인하는 것으로는 부족하다 — **애초에 어떤 채널이 있는지를 모르면 범위조차 못 잡는다.**
👉**순서 = ①링크 허브 → ②거기 걸린 채널을 목록화 → ③그다음에 수확 계획.**

🪤**리틀리는 「읽는 재료」로는 1순위지만 「아티클 링크」로는 금지다**(§④ 참조 — 목록이라 「아티클 보기」와 안 맞는다). 헷갈리지 마라.
📌덤으로 **대표 URL 후보·상품 목록·예약 채널**이 한 화면에 다 있어서 폼 입력이 빨라진다.

### 🚨 인스타 수확 = **전량 수확이 기본이다** (08-05 대표 확정)

**이 스킬은 동의 후에만 시작한다** — 영업 플로우가 "만들어드릴까요? → 예스"를 거친 뒤 제작에 들어가므로,
시작 시점엔 항상 동의가 있다. *"완전히 실전으로, 정확한 정보로"*(대표 원문).

**절차**: 대표가 **인스타그램 탭을 열고 본인 계정으로 로그인**해두면 → 그 세션으로 그리드 전량 열거 →
내부 웹 API로 캡션 전문·작성일·태그계정·원본 이미지 URL 조회. 기법 정본 = [[collab-page-enrich]] §②-A(콜마페 124건 실증).

⭐🆕**수확 대상은 넷이다 — 캡션만 받고 끝내지 마라** (08-25 개정)
| | 어디서 | 왜 빠뜨리면 안 되나 |
|---|---|---|
| ① 피드 캡션 | §A1-alt | 통상 글의 **대부분**이 여기 있다(대표 08-25) |
| ② 태그된 게시물 | §A3-b | **「남이 연 행사」**는 그리드에 없다 |
| ③ **하이라이트** | §A5 | 상시 프로그램·후기·안내가 여기 굳어 있다 |
| ④ **이미지 속 글자(OCR)** | **§A6** | 캡션에 없는 말이 여기 있다. **③은 이게 사실상 전부다** |

🪤③④를 빠뜨리면 **「없다」가 아니라 「안 봤다」인데 「없다」로 보고하게 된다** —
1팀이 콜마페 캡션 126편을 두 번 훑고도 **가게 이름의 유래**를 못 찾았다(이미지 안에 있었다).

✅**08-25 「터」에서 첫 실전** — 계정 3개 · 이미지 **1,184장** · OCR 게이트 통과 44% · **0원 · OCR 3.5분**.
🎯가장 값이 컸던 건 **하이라이트 「기부」 4장**이었다. 캡션 104건 어디에도 없던 이야기(첫 종이책 수익 전액 기부,
휠체어 경사로 활동 후원)가 거기 있었고 **그게 브랜드의 성격 자체**였다.

🚨⭐**하이라이트가 「본체」인 경우가 있다 — 수확 계획을 잡기 전에 개수부터 세라.**
터의 브랜드 계정은 **게시물이 8건**뿐이었는데 대표 개인 계정 하이라이트가 **15개 731장**이었다.
피드(338장)의 **두 배**다. `highlights_tray` 한 번이면 세어진다.
🔒단 그중 **469장이 회원 사진**(후기·비포애프터)이었다 — **받되 소개서엔 쓰지 않고, 개수와 이유를 보고한다.**

🛠도구 = `scripts/ocr/ocr-text.swift`. 최초 1회 `swiftc -O scripts/ocr/ocr-text.swift -o scripts/ocr/ocr-text`.
🪤**배치 스크립트를 쓸 땐 출력 파일명을 폴더별로 갈라라** — 08-25에 계정 두 개를 같은 `ocr.jsonl`로 써서
**먼저 돌린 결과를 덮어썼다.** 다시 돌리면 되는 값이라 손해는 없었지만, 원본이었으면 사고였다.

- 강도 상한(정본 = [[ig-photo-tool]]): **요청 간 1~2초 · 계정당 130건/일(🔄08-05 대표 상향, 구 100) · 하루 2~3계정.**
  게시물이 그보다 많으면 최신부터 130건에서 끊고 보고한다(다음 날 이어받기).
  🚨🆕**그리고 「어디서 끊겼는지」를 «날짜»로 적어 둬라** (08-25 터 건). 끊긴 바깥은 **「없다」가 아니라 「안 받았다」**인데,
  후보를 판정할 땐 그 구분이 사라진다 — 나는 빙빙클래스를 *「게시물 한 건뿐이라 무엇을 했는지가 안 잡힌다」*로 🟡을 달았고,
  **대표가 준 글 하나**로 뒤집혔다(마인드맵 클래스였다. 그 글은 내 수확 하한 3주 밖에 있었다).
  ⭐**규칙 = 「N건뿐」이라고 쓰기 전에 «내 수확 범위가 그 항목의 시기를 덮는가»를 본다.** 안 덮으면 그 항목만 이어받아 더 받는다.
  🔗[[index-is-not-the-source]]·[[dont-narrow-before-the-owner-sees]]의 **수확 상한판**이다.
- ⛔ 대표 세션 없이 시작하지 않는다. 로그인월 우회·UA 위장 금지. 막히면 "막혔다"고 보고.
- 🤝 **인스타 작업 전 2팀에 DM** — 레이트리밋은 IP=머신 공유(08-04 실측, 1팀 302 사건). 겹치면 서로 결과를 오염시킨다.
- (예외) 동의 **전** 사전 검토가 필요한 특수 케이스만 저강도 공개 수집(바이오+10~15개·4초) — 이 스킬의 정규 경로 아님.

## ① 사전 리서치 → 브랜드 브리프 (유료 0콜)

수확(기법 정본 = [[collab-page-enrich]] §②): 인스타 바이오·캡션·**하이라이트**·**이미지 속 글자(OCR §A6)**(위 강도 내) + 홈페이지
+ 필요시 네이버 지역검색·기사. **진행하며 요약을 쌓는다** — 끝나면 아래 형식의 **브리프**로 정리해 대표에게 먼저 보여준다.

### 📏 홈페이지 fetch — 두 층이 따로 돈다 (08-05 코드 실측 · `src/lib/homepage.ts`)

같은 홈페이지를 **두 번 읽는다.** 층이 다르고 상한이 다르다 — 헷갈리면 "왜 모델이 이 내용을 몰랐지"를 오진한다.

| | ⓐ **이 스킬의 사전 리서치** (①, 브라우저/사람) | ⓑ **제품 서버 딥리드** (②의 위저드가 자동 실행) |
|---|---|---|
| 언제 | 위저드 **전** — 브리프 재료 | 위저드 ③에서 홈피 "맞아요" 확약 시 |
| 페이지 수 | **15~20p까지**(대표 08-05 "더 많이") — 의미 있는 페이지는 다 읽는다 | **최대 10p**(홈 포함) — 코드 고정 |
| 시간 | 사람 페이스 (수 분 — 대표 컴이라 무관) | **전체 8초** 예산·페이지당 4초(늦으면 포기) |
| 분량 | **페이지당 5,000자 · 총 50,000자**(08-05 대표 확정 — ⓑ의 ~2.8배) | 페이지당 3,500자 → **총 18,000자**에서 자름 |
| 서브 페이지 선택 | 사람이 메뉴 보고 판단 | 링크 점수제: **소개·스토리·철학 100점** > 콜라보·파트너 65 > 프로그램·서비스 60 > 뉴스·연혁 30 > 팀 25. 로그인·장바구니·약관은 차단 |
| 못 읽는 곳 | 없음(브라우저라 JS 렌더도 읽음) | 네이버 계열·modoo·인스타·페북 등 **시도 자체 안 함** + JS 렌더 사이트는 빈손 |
| 소비자 | **사람**(브리프 → 칩·별표·게이트 조작) | **모델**(생성 프롬프트에 [홈페이지—발췌]로 주입) |

- ⭐**ⓐ를 늘려도 ⓑ는 안 늘어난다** — 모델이 직접 읽는 건 ⓑ의 18,000자뿐이다. ⓐ에서 발견한 것을 모델에 전하는 통로는
  **칩 직접 추가·별표·사실 게이트**(사람 손)뿐이라는 걸 잊지 말 것.
- ⓑ의 상한을 코드에서 올리는 건 **비용·지연과 직결**이라 별도 결정 사항이다(입력이 이미 원가의 절반 — 딥리드 압축이 백로그).
  modoo·네이버 홈페이지 브랜드는 ⓑ가 0이므로 **ⓐ가 유일한 홈페이지 경로**다.
- ⓑ 확장은 **08-05 대표 문의에 "지금은 안 늘린다"로 답함** — 10p 점수제가 이미 알짜를 뽑고, 글자 확장은
  입력 원가·위저드 지연만 키운다(증거 없음, 07-27). ⏭단 **무비용 재배분 실험**은 열어둠: 총량 18,000 유지 +
  최고점 페이지(소개·스토리)만 페이지당 3,500→6,000자. **테스트 1회차에서 스냅샷 A가 얇으면 그때 근거 갖고 재론.**

```
[브랜드 브리프 — ○○○]
정체:        (무엇을 하는 곳인가 한 줄)
사장님 목소리: (바이오·캡션·**이미지 속 글자**에서 원문 그대로 2~4개 — verbatim 후보)
  🚨**이미지에서 온 말은 화자를 확인하고 뽑아라** — 인터뷰 카드는 **질문이 남의 말**이다([[who-authored-the-field]], §A6 함정①).
⭐본인이 붙인 이름: (프로젝트명·서비스명·자기 별칭 — 원문 표기 그대로. ⑤ 문안의 1급 재료)
활동 후보:    (반복·대표 프로그램 우선, 일회성도 브랜드다움 보이면)
콜라보 후보:  (파트너명이 드러난 것만)
키워드 후보:  (칩으로 쓸 것 — 아래 ② 기준으로 미리 걸러둠)
확인된 사실:  (수상·방송·수치 등 — 별표·사실게이트 후보)
사진 후보:    (게시물 URL 목록 — ⑤에서 회수할 것)
불확실:      (확인 못 한 것 — 위저드에서 켜지 말 것)
```

### 🚨 리서치 검색은 **전문에 대고 하라** — 요약본은 검색도 못 믿는다 (08-18)

수확물의 캡션은 **잘려 있을 수 있다.** 전종원 작가님 수확본은 **400자에서 컷**돼 있었고
146건 중 **48건이 그 상한에 걸려** 있었다. 결산형 게시물(한 편에 소절 넷)은 뒤쪽 소절이 통째로 사라진다.

🪤**그래서 「그 주제 게시물은 하나뿐」이라는 오답이 나온다** — 실제로는 **4건**이었다(08-18, 「네 멋대로 해라」).
⭐**규칙 = 후보를 훑을 때가 아니라 「몇 건인가」를 답할 때는 전문을 확보하고 세라.**
- 판별: `cap.length`의 분포를 본다. **한 값에 뭉쳐 있으면 그게 컷 지점**이다(48건이 정확히 400).
- 전문 재조회 = `/api/v1/media/{id}/info/`(shortcode → base64 디코드). 48건에 1분 안쪽.
- 🔗**이 함정의 상위 규칙** = memory `index-is-not-the-source`(요약본으로 고르고 원문으로 쓴다).
  **검색까지 요약본에 대고 하면 「없다」는 결론 자체가 틀린다.**

## ② 폼 직접 입력 — **위저드를 돌리지 않는다** (대표 확정 08-21, 「ⓐ안」)

### 왜 폐기했나
- 🚨**위저드는 우리 리서치를 못 읽는다.** 자기 크롤만 본다 — 인스타 636건 전량 수확본이 있어도 소용없다
- 유료 콜인데 **우리 브리프보다 얕은 결과**가 나온다
- ⭐위저드의 남은 값은 **「고객이 혼자 만들면 어디까지 나오나」의 하한 측정**뿐이다
  (그 용도는 [[collab-brandpage-creation-basic]] 모드 A)

### 폼 구조 — 저장 전과 후가 다르다 (08-21 실측)

| 화면 | 여기서 넣는 것 |
|---|---|
| `/register` **신규** | 이름 · 한줄 · 자세히 · **사진(필수 1장)** · **콜라보 유형 칩(공용 1세트)** · 찾는 설명 · 제공 설명 · 주소 · 인스타 · 대표 URL |
| 등록 버튼 누르면 뜨는 **모달** | 활동 · 콜라보 · 고객 (우하단 **「+ 추가」** 플로팅 버튼에도 같은 메뉴) |
| 저장 후 `/register?edit={slug}` | **시작 이야기**(「왜 이 브랜드를 시작하셨나요」) · **키워드 칩**(「브랜드를 표현하는 키워드」) · 블록 6종 |

🚨**시작 이야기와 키워드는 신규 화면에 없다.** 저장하고 수정 모드로 다시 들어가야 나온다.

### 개수·정렬
- **상한 활동 30 · 콜라보 30** (`src/lib/limits.ts` — 숫자를 여기 쓰지 말고 그 파일을 열어라).
  🪤스킬 문서의 낡은 숫자(5·10)를 믿고 대표께 오보한 적 있다(08-21)
- **정렬 = 「한 일의 시점」 최신순**(대표 확정 08-21). 「글을 쓴 날」이 아니다 —
  *「소개서를 보는 사람은 지금 뭘 하시나를 본다」*
- 화면엔 5개씩 보이고 나머지는 「N건 더 보기」로 접힌다(`REVEAL_STEP`)

### 🚨🆕 폼을 만지기 «전에» — **screenshot 한 장으로 화면이 그려졌는지 확인한다** (08-25 오진)

⛔**백그라운드 탭은 페인트가 멈춘다.** 그 상태로 DOM을 읽으면 **「멈췄다」로 보인다.**
08-25에 이걸로 **prod 장애로 오진하고 1팀에 「고객이 소개서를 못 만든다」고 알릴 뻔했다.**

**내가 본 증상 → 실제**
| 관측 | 내 결론 | 진짜 |
|---|---|---|
| 스피너 21초+ · 새 탭·새로고침 동일 | 「폼이 안 뜬다」 | 화면만 안 그려짐 |
| 콘솔 에러 **0** · 네트워크 **전부 200** | 「조용한 실패」 | **아무것도 안 깨졌으니 당연** |
| 숨겨진 div에 **입력칸 11개가 다 있음** | 「서버는 보냈는데 교체가 안 됨」 | **데이터는 도착해 있었다** |

⭐**판별 신호 셋 — 겹치면 rAF 정지를 먼저 의심하라**(1팀 08-25 정리):
① 스피너가 **비현실적으로 오래** 돈다 ② **콘솔 0·네트워크 200** ③ **DOM엔 내용이 이미 있다**(결정적)
🔑**`document.visibilityState`를 한 줄 찍으면 1초에 갈린다.** `hidden`이면 페인트 문제다.
👉처방 = **`computer{action:"screenshot"}` 1회**(강제 페인트). 그러면 즉시 정상 렌더된다.

🪤**대조군을 만들 땐 변수를 하나만 바꿔라.** 나는 「내장 브라우저·로그아웃」 vs 「대표 크롬·로그인」을 비교하고
**「로그인 때문」**이라고 좁혔다 — **브라우저도 달랐다.** 1팀이 «같은 브라우저·같은 로그인·포그라운드»로 재보고 갈랐다.
🔗memory `verify-the-control-case` · `self-inflicted-symptom-attribution`

🪤**`find`·`read_page`는 `document_idle`을 기다린다** — 페이지에 도는 애니메이션(스피너)이 있으면 45초 타임아웃이 난다.
**`javascript_tool`은 그 상태에서도 동작한다.** 도구가 막히면 「페이지가 죽었다」가 아니라 **「idle이 아니다」**를 먼저 의심하라.

### 조작 기법 (대표 크롬 fresh 세션에서만)
- 🚨**저장은 대표 실크롬에서만 된다.** 내장 브라우저는 폼이 다 채워져도 **저장만 조용히 실패**한다
- **텍스트**: React 네이티브 setter — `Object.getOwnPropertyDescriptor(Object.getPrototypeOf(el),'value').set.call(el,v)` 후 `input`·`change` 디스패치
- **칩**: 값 주입 후 **Enter keydown 디스패치**. ⚠️**하나씩**(연달아 넣으면 앞엣것이 씹힌다).
  선택되면 목록에서 사라지고 `✓`가 붙는다. **키워드는 10개 상한**(꽉 차면 입력칸 `disabled`)
- **카드 추가**: 「＋활동 추가」·「＋콜라보 추가」를 **간격 0.35초로 반복 클릭**해 필요한 수만큼 만든 뒤 한 번에 채운다
- **링크**: 카드마다 「＋아티클 링크 추가」를 눌러야 칸이 생긴다. **반드시 `https://` 포함 전체 URL**
  (서버가 스킴 없는 값을 조용히 버린다 — 저장은 성공하는데 링크만 사라진다)
- **콜라보 전용**: 연도 `<select>` + 유형 칩 7종 + 「+ 유형 직접 추가」
- **최종**: 「브랜드 소개서 등록하기」/「수정 완료」. 클릭 후 `/m/{slug}`로 바뀌면 성공.
  ⚠️**재클릭 남발 금지**(POST가 서로 abort). 안 넘어가면 스샷 확인 후 좌표 실클릭
- 🚨**수정 모드 진입 시 임시저장 배너가 뜨면 반드시 「저장된 내용 그대로」.**
  「이어서 쓰기」는 서버 최신본을 낡은 로컬 초안으로 덮는다

### 🔗 아티클 링크 — 고르는 기준은 하나다
활동·콜라보의 링크는 **1개뿐이고 라벨이 「아티클 보기」로 고정**돼 있다(`MakerArticle.tsx`).
⭐**그러니 「아티클 보기」라고 써 있어도 어색하지 않은 것 = 읽을 거리만** 넣는다.

1. **본인이 직접 쓴 정리 글** — 브런치 1순위, 블로그 2순위
2. 인스타는 **모집 공고 말고 「끝난 뒤 기록·후기」**
3. 파트너가 쓴 글은 마지막(남의 집이라 내려갈 수 있다)

⛔링크트리·리틀리·신청 폼은 넣지 않는다(목록이라 「아티클 보기」와 안 맞고 항목마다 같은 주소가 붙는다).
여러 개가 꼭 필요하면 **선택 블록**으로(links 3개 + 이름표 자유).

🚨**주소는 반드시 수확본에서 찾아 붙여라.** 08-21에 날짜는 데이터에서 보고 **주소는 기억으로 적어
8개를 지어냈다**(대표가 눌러보고 발견). 넣기 전 검증:
```bash
curl -A "facebookexternalhit/1.1" https://www.instagram.com/p/{code}/ | grep -c og:description
# 1 = 실재 / 0 = 없는 페이지 (HTTP는 없는 것도 200을 준다)
```

## ③ 사진 1장 + 저장 = 📸 스냅샷 A

- 🚨 **신규 등록은 사진 1장 필수**(08-05 배포). ✅**사진 자동 첨부는 2026-08-21에 뚫렸다** —
  크롬 확장 `file_upload`로 되고, **파일을 이 세션의 scratchpad에 두는 것이 관건**이다(절차·검증 = [[collab-page-enrich]] §④-B).
  대표가 직접 올리는 것도 여전히 가능하다.
  → **대표가 브리프 「사진 후보」에서 1장을 골라 직접 올린 뒤** 저장한다. (대표 부재면 여기서 멈추고 대기.)
- 저장 전 **폼 전체 스크린샷 + 프리필 텍스트**를 보관 = **스냅샷 A** ("제품이 혼자 만들면 여기까지"의 증거).
- 저장·검증·slug·소유권 = [[collab-brandpage-creation-basic]] §4·§6 그대로.

## ④ enrich 이어달리기 — 후보 선별 (유료 0콜)

[[collab-page-enrich]] ①진단 → §②-B 후보 랭킹(활동 30·콜라보 30 — 08-21 대표 정정, 우리가 먼저 좁히지 않는다) → §③-A **아티팩트 후보 페이지**로 대표에게.
⚠️ **리서치는 ①에서 이미 했다 — 재수확하지 말고 브리프·수확물을 재사용**한다(이 스킬의 존재 이유).
⛔ ③은 하드 게이트 — **대표 픽 없이 prod 수정 금지.**

### 🚨🆕 ④-A **아티팩트에는 소개서의 «모든 칸»을 넣는다** (대표 지시 08-25, 터 건에서 4칸 누락)

대표: *「스샷에 있는 영역들도 준비해줘. 이건 이번에 스킬에 못박자, 왠지 스킬에 안 들어간 거 같아.」*
🪤내가 올린 1차 아티팩트엔 **한 줄·자세히·활동·콜라보 넷뿐**이었다. 소개서에는 **섹션이 아홉**이다
(`MakerArticle.tsx` 실측) — **네 칸을 통째로 빼놓고 「후보 다 냈다」고 보고**했다.

| 소개서 섹션 | 필드 | 아티팩트 `kind` |
|---|---|---|
| 우리는 이런 브랜드에요 | `oneLiner` · `intro` | 한 줄 소개 / 자세히 소개 |
| **이런 콜라보를 찾고 있어요** | `seeksDescription` (+ 아래 🚨 공용 칩) | **섹션** |
| 우리는 이런 일을 하고 있어요 | `activities` | 활동 |
| 우리 브랜드의 시작 | `story` | 섹션 |
| 함께한 콜라보 | `collabHistory` | 콜라보 |
| **이런 콜라보를 제공할 수 있어요** | `offersDescription` (+ 아래 🚨 공용 칩) | **섹션** |
| **저희는 이런 고객들과 함께 하고 있어요** | `targetAudience` 칩 | **섹션** |
| **우리를 표현하는 키워드에요** | `keywords` 칩(최대 10) | **섹션** |
| 소개된 곳 / 선택 블록 | `showcases` | 소개된 곳 |

- 🚨🆕**유형 칩은 «한 세트»다 — 찾는/제공으로 나뉘어 있지 않다** (코드 주석 08-25 확인)
  `register/page.tsx`의 `#offers-chips` 하나뿐이고 **`offers`에 저장된다.** 주석 원문:
  *「협업 유형 칩 — 공급·수요 통합 1세트(2026-07-22 대표 확정 — 검색이 이미 offers∪seeks OR라 구분에 실체 없음). 구 seeks 칩은 은퇴」*
  👉**아티팩트에서 「찾는 칩」·「제공 칩」을 따로 제안했으면 폼에 넣을 때 «합집합»으로 만든다.**
  🪤08-25에 이 표에 둘로 적어놨다가 폼 앞에서 어긋났다 — **칸 목록은 코드를 열어 맞춘다.**
- ⭐**칩이 들어가는 칸은 「문안 + 칩 목록」을 같이 낸다.** 유형 칩 7종은 `src/lib/types.ts`의 `COLLAB_TYPES`를 열어 확인하라
  (제품콜라보·팝업·워크숍·공동굿즈·공동콘텐츠·행사참여·공간대여). ⛔외워서 쓰지 마라.
- 🏷**`targetAudience`는 라벨이 아니라 «상태»로 쓴다** — ⛔`운동 초보자` ⭕`운동이 처음이라 무엇부터 해야 할지 모르겠는 분`
- ✂️**`keywords` 칩은 짧게 쪼갠다**(대표 08-25 실측 1건) — ⛔`아프지 않게 잘 움직이기` ⭕`아프지 않게` + `잘 움직이기`
- 🔗⑥ 완성 체크 표와 **같은 목록**이다. 거기서 세기 전에 **여기서 후보로 냈어야** 대표가 한 번에 본다.

### 🔴🔴 ④-B 후보를 아티팩트에 올리기 전 — **네 단계를 반드시 거친다** (08-23~24 방혜리 건에서 확립)

후보 55개를 냈다가 대표가 *"설명이 너무 간단해 보인다"* → *"이거 현재까지 하고 있는 거 맞아?"* →
*"연관 아티클을 오늘 다 안 읽었잖아"* 로 **세 번 되돌린** 끝에 나온 절차다. 하나라도 건너뛰면 그 자리에서 걸린다.

#### 1️⃣ 근거로 붙일 글은 **전문을 읽는다** — 키워드 매칭은 근거가 아니다
🪤08-24에 근거 링크 67건을 붙였는데 **그중 여럿은 「그 글에 이 기관 이름이 나온다」까지만 확인한 것**이었다.
대표가 *"게시글 원문 다 읽은 상태 맞지?"* 하고 물어 전수로 다시 읽으니 **바로 오류가 나왔다**:
- 「밀양은대학」으로 걸린 글은 **다른 사람(심오한연구소 「심바」)의 학과 모집 공고**였다 → 근거에서 뺐다
- 「서대문」으로 걸린 글은 **서대문공동체라디오**였다(전혀 다른 건)
- 「전직」으로 걸린 글은 **「전직 동료」**였다
⭐**규칙 = 항목에 링크를 붙이기 전에 그 글을 열어 끝까지 읽는다.** 못 읽었으면 붙이지 않는다.
🔗상위 규칙 = memory `index-is-not-the-source`.
- 🚨⭐**「원문과 다르다」고 판정하기 전에 다른 출처도 열어라.** 08-24에 인스타 캡션이 〈일잘러의 **기술**〉이라
  제목 〈일잘러의 **조건**〉이 틀렸다고 지적했는데, **홈페이지 강의이력엔 「조건」으로 등록돼 있었다**(둘 다 쓰인 이름).
  한 곳만 보고 「틀렸다」 하면 **맞는 걸 고치게 된다.** 🔗memory `verify-the-control-case`

#### 2️⃣ **같은 일을 다루는 글을 전부 모아 붙인다** — 한 건만 붙이면 시점이 굳는다
대표 지시(08-24): *"원문보기 1개만 노출하지 말고, 연속이거나 참고한 링크가 있다면 하단에 판단에 사용한 아티클 리스트를 모두, 게시날짜와 함께."*
- 이유 = **대표가 「지금도 하고 있나」를 그 자리에서 판단한다.** 링크가 하나뿐이면 그게 2024년 글일 때 살아 있는 활동도 죽은 것처럼 보인다.
  (월간회고클럽은 2024-06 첫 모집부터 **12건**이 이어져 있어서 *"지금도 하는 거 맞아?"* 에 그 자리에서 답이 됐다)
- 템플릿 필드 = `sources: [['YYYY-MM-DD','shortcode'], …]` **최신순**. `review-template.html`이 「판단에 쓴 글 N건」으로 렌더한다.
- ⛔**브랜드 계정의 「고정 소개글」은 근거에서 뺀다** — 전 활동을 다 나열하고 있어 아무 항목에나 걸리고, 32개 항목에 같은 링크가 붙어 무의미해진다.
- 📏**「전부」의 상한 = 최신 5건.** 그 이상이면 **자르되 총량과 기준을 같이 적는다** — `판단에 쓴 글 12건 중 최신 5건` 처럼.
  ⛔**말없이 자르지 마라**(1팀 되물음 08-24 · memory `dont-narrow-before-the-owner-sees`) — 잘린 게 있는지조차 대표는 알 수 없다.
  ⭐한 항목에 12건이 붙는 일이 실제로 있었고(월간회고클럽), 그때 **「12건이 이어져 있다」는 사실 자체가 답**이었다. 개수는 지워도 총량은 남겨라.
- ⭐**기관이 올린 주차별 기록을 무시하지 마라.** 동해시 항목은 기관 계정의 18건을 다 읽고 나서야
  **「한 기수에 진로특강 3회기」**라는 구조가 나왔다. 본인 피드엔 그 얘기가 한 줄도 없었다.

##### 🌐**인스타 밖에서 온 사실은 출처를 따로 보여라** (대표 지적 08-24)
대표 질문: *"여기 있는 20명은 어떤 자료에서 온 거야?"*
🪤**숫자는 맞았다.** 홈페이지 강의이력 표에 「브라이언임팩트 · NPO채용사업 1:1 채용서류 컨설팅 ▸20명」이 그대로 있었다.
**틀린 건 아티팩트였다** — 근거로 인스타 목록 글만 보여주고 있었는데 **그 글엔 숫자가 없다.**
대표 눈에는 **없는 근거를 붙여놓고 숫자를 쓴 것**으로 보인다. 못 믿는 게 정상이다.
- ⭐`web: ['자료명 — 그 줄 그대로']` 필드로 **링크가 아니라 「어느 자료의 어느 줄」을 그대로** 보인다. 대표가 그 자리에서 대조할 수 있어야 한다.
- 🤖게이트 검사: **숫자를 썼는데 근거가 하나도 없으면 FAIL.**
  08-24 실측 = 인원수 7건(15·40·20·30+·15·20·5명)이 **전부 홈페이지 표에서 왔는데 화면엔 인스타 링크만** 있었다.
- 📌재료 파일에 **원문 그대로**(`hp-reference.html`) 남겨둔다. 요약본만 두면 이 대조를 못 한다.

#### 3️⃣ 🧪**레드팀 패스 — 문장마다 「이 말이 어느 줄에서 왔나」를 되묻는다.** 못 대면 지운다
대표가 *「토마토스튜를 매번 나눠 먹는 거야?」* 한 마디로 잡아냈고(관찰은 **1회**뿐이었다),
그 지적을 받고 **전 항목을 기계적으로 훑으니 같은 오류가 26건** 나왔다(비약 6 + 근거 없는 연결 문장 20).
🪤**이 오류는 읽어서는 안 걸린다.** 사실과 사실 사이를 그럴듯한 말로 메운 것이라 문장만 보면 자연스럽다.
잡히는 건 **출처를 하나씩 되물었을 때**뿐이다. 아래 넷을 순서대로 센다:

| | 무엇을 보나 | 08-24 실제 사례 |
|---|---|---|
| ⛔**한 번 본 것을 습관형으로** | 관찰 1건인데 `~해요`로 쓰지 않았나 | 「그날은 토마토스튜를 나눠 먹어요」(참여자 후기 1건) |
| ⛔**역할 추정** | 주최/공동/게스트를 짐작하지 않았나 | 각할모를 「공동 호스트」로 썼는데 **파랑~ 님 모임의 게스트**였다 |
| ⛔**남의 일을 내 일로** | 주최 측이 한 일을 옮기지 않았나 | 「공간 세팅하고 포스터 붙이고」는 **주최 아지트레츠**가 쓴 글이었다 |
| ⛔**옆 항목 사실 차용** | 다른 항목의 사실을 끌어오지 않았나 | 고민정거장 콜라보의 「봄부터 공동기획」을 **별개 건인 성북 워크숍**에 붙였다 |
| ⛔**남의 활동을 내 활동으로** | 제목 말고 **화자**를 봤나 | 태그 피드의 「뉴그라운드 북클럽」은 **다른 사람이 자기 책으로 연 모임**이고 우리 쪽은 참석자로 태그된 것이었다 |

- 🗣**화자 판별 문장 = *"우리가 할 수 있나"*❌ **"우리가 맡은 몫이 뭔가"**⭕** (1팀 08-12).
  🔁한 규칙의 양 끝이다 — 08-12는 **몫을 부풀린 것**(참여자가 가르친 기술이 우리 특징으로 둔갑),
  08-24는 **남의 일을 통째로 가져올 뻔한 것**. 🪤태그 피드는 수확량만 늘리는 게 아니라 **가짜 후보를 만들어 낸다.**

- 🚨⭐**다른 소개서와 충돌하는지 본다.** 같은 모임·같은 파트너가 이미 다른 소개서에 있으면 **그쪽을 열어 맞춘다.**
  각할모 건을 안 잡았으면 파랑~ 님 소개서와 **같은 모임을 두고 서로 다른 말**을 할 뻔했다 —
  한쪽이 틀린 게 아니라 **둘 다 신뢰를 잃는** 종류의 사고다. 우리 소개서가 늘수록 이 충돌은 잦아진다.
- ⚠️🆕**단 「지운다」는 «근거가 아예 없을 때»다.** 부분적으로 참인 문장은 **먼저 층을 낮춰 본다**(§✍-20).
  ⛔「반증됐으니 버리자」는 과잉 교정이 되기 쉽다 — 08-25에 내가 그렇게 조언했고 대표가 **연결을 살려** 되돌렸다.
- ⭐**지어낸 문장을 지우면 항목이 짧아진다. 그게 정답이다.** 08-24에 20건을 걷어내니 17건이 85자 아래로 내려갔다.
  **채우지 말고 「⚠️재료=목록 한 줄」로 표시해 대표에게 보인다** — 지어내서 채우는 것보다 짧은 게 낫다.
  (그중 3건은 재검토에서 진짜 사실이 나와 되살아났다. 「전국 청년/장년 대상」·「특성화고 취업 프로그램」·「러닝 퍼실」)
- 🚨⭐**「없다」는 검사 결과여야 한다 — 검색을 돌린 뒤에만 쓴다.**
  08-24에 「소개된 곳」 두 건에 *"인스타에 관련 글이 없어 웹에서 확인한 사실만 적었습니다"* 라고 딱지를 붙였는데 **둘 다 글이 있었다.**
  안 찾아보고 쓴 것이다. 게다가 그 글엔 쓸 재료가 있었다 — 「크레마클럽 1위 → **그 성원으로 종이책 출간 확정**」이라는 인과가 통째로 들어 있었다.
  🪤**틀린 「재료 부족」 딱지는 사실을 숨긴다** — 딱지가 붙으면 대표는 안 보고 넘어간다. 🔗memory `self-inflicted-symptom-attribution`

#### 4️⃣ 🚦**게이트 — 통과 못 한 항목은 「딱지를 붙여」 보낸다** (08-24 방혜리 2차)

1️⃣2️⃣3️⃣이 **검사**라면 4️⃣는 **판정**이다. 왜 필요한가 —
대표 진술: *"내가 아티팩트 초안을 보고 부족함을 느껴 스샷을 주면 → 너가 원문을 다시 보고 초안을 다시 주고 → 내가 거기서 고치고 → 다음으로"*
**항목마다 왕복 1회**가 든다. 방혜리 님은 활동만 29건이라 왕복이 그대로 시간이 됐다.
🎯목표 = 대표가 **고르는 일**만 하고 **고치는 일**은 우리 안에서 끝낸다. (장기 목표: 소개서 1건을 사진까지 2시간)
✅08-24 실측 = 이 게이트를 붙인 3차 초안에서 **대표가 활동 21건 중 2건만 빼고 나머지는 제목·문체 다듬기로 끝냈다.** 대표 평: *"검증과정으로 만드니 훨씬 나아졌어"*

##### ⭐대표가 실제로 고쳐 온 축 셋 — 이 순서로 본다
대표 진술: *"길거나 짧으면 수정 → 말의 톤을 수정 → 단순 과정만 이야기한다면 알맹이나 목적, 소중한 점을 짚어주는 순"*

| 축 | 대표가 고치는 것 | 내가 미리 잡을 것 |
|---|---|---|
| **길이** | 너무 길거나 짧음 | 🚨**숫자 기준을 쓰지 마라** — 아래 §📏 참조. 「무엇을 했는지가 잡히나」로 본다 |
| **톤** | 「~이에요」로 끝나는 명사 서술 | 「~을 진행했어요」로 **형식을 명명**하며 연다 |
| **알맹이** | 과정만 나열 | 목적·의도·소중한 점이 **한 번은** 나와야 |

##### 📏🚨 길이 규칙은 **폐기했다** (08-25, 확정본 104건 전수)
옛 규칙을 **대표 확정본 5개 브랜드 104건**에 돌려 보니 이렇게 나왔다 —

| 옛 규칙 | 확정본이 걸리는 비율 |
|---|---|
| 콜라보 200~250자 | **76%** |
| 「두 문단」 | **73%가 1문단** |
| 활동 100~180자 | **55%가 범위 밖** |
| 85자 미만 = 미완성 | 25% |

⭐**원인 = 브랜드마다 길이가 다르다.** 활동 desc 중앙값이 **캔가 95자 · 전종원 153 · 파랑~ 140 · 방혜리 168 · 계단뿌셔 288** — **3배 차이**다.
옛 수치는 전부 **방혜리·파랑~ 건에서 뽑은 값**이라 다른 브랜드에 대면 틀린다.
🪤**그리고 규칙이 나쁜 글을 만들었다** — 대표(08-25): *「글자수 제한이 있어서 이렇게 하는 거면 글자 제한이 잘못된 거 같아」*.
콜라보 200자를 채우려고 **의미 없는 둘째 단락**을 붙이고 있었다.

👉**하한도 상한도 두지 않는다.** 대표: *「차라리 좀 짧되 맞고 필요하고 중요한 말 위주로」*.
길이는 **재료가 정한다** — 재료가 적으면 100자로 끝내고, 그게 정상이다.
✅남는 검사는 **상한 경고 하나뿐**(250자 넘으면 운영정보·감상이 섞였는지 본다. 넘었다고 자르지는 않는다).

##### 🚦딱지 셋 — 버리지 말고 표시해서 보낸다
- 🟢 **통과** — 원문과 대조했고 규칙도 맞다. 대표가 빠르게 넘겨도 되는 것
- 🟡 **재료 부족** — ⛔채우지 않는다. 문구는 **「뺄지 판단해주세요」**로 쓴다(아래 참조)
- 🔴 **판단 필요** — 내가 사실을 틀렸거나, 대표가 정해야 할 게 있다. **대표는 여기부터 본다**

⭐**딱지의 목적은 「무엇을 안 봐도 되는지」를 알려주는 것이다.** 08-24 실측 = 33건 중 🟢16 🟡14 🔴3.
🚫**통과 못 한 걸 말로 메워서 통과시키지 마라.** 그게 3️⃣이 잡으려는 지어낸 연결 문장이다.

##### ⭐🟡의 진짜 뜻은 「짧게 둔다」가 아니라 **「뺄 후보」**다
08-24에 대표가 🟡 두 건을 **뺐다.** 그런데 판정축이 재료의 **양**이 아니었다 —
- ⛔뺌: 경기청년공동체 「팀빌딩·역량발견 워크숍」 → *"뭔가 컨텐츠가 불명확해"*
- ⛔뺌: 한양대 「커리어 로드맵 설계 컨설팅」 → *"클루가 너무 적어서. 이력은 맞는 것 같으나 명확한 증거가 웹에 없으니"*
- ⭕남김: 브라이언임팩트 「스무 명 채용서류 컨설팅」 — **같은 「목록 한 줄」인데 살았다**

👉**판정축 = 「무엇을 했는지가 한 문장으로 잡히나」.** 잡히면 짧아도 살고, 안 잡히면 길어도 죽는다.
그러니 🟡 문구는 *"짧은 채로 두었습니다"*가 아니라 **"뺄지 판단해주세요 — 무엇을 했는지가 안 잡힙니다"**로 쓴다.

##### 🤖게이트는 사람 눈이 아니라 코드로 돌린다
「세는 점검을 읽는 점검보다 먼저」(memory `brandpage-copy-payoff`). 실제로 쓴 검사 —
```js
const n = (it.draft || '').length;                      // 공백 포함
if (n > 250) WARN(it.id, `${n}자 — 운영정보·감상이 섞였는지 확인(길다고 자르진 않는다)`);
// 🚨08-25 정정 — «명사 서술 금지»는 폐기했다. 확정본 104건에 돌리니 15건이 걸렸다
//    (「~워크숍이에요」「~프로그램이에요」「~콜라보예요」는 전부 «정상적인 정의문»이다).
//    대표가 지적한 건 어미가 아니라 ①홍보 문구로 연 것 ②시제가 어긋난 것, 둘이었다.
const paras = (it.draft || '').split(/\n\n+/).filter(x => x.trim());
const PROMO = /(모집|신청|마감|참가비|선착순|영업 시작|많관부|프로필 ?링크)/;
if (PROMO.test(paras[0].split('.')[0])) FAIL(it.id, '첫 문장이 홍보·모객 문구다');
// 시제 일치 — 본문이 «끝난 일»인데 마지막이 현재형 명사 서술이면 어긋난다
const last = (it.draft||'').replace(/\n/g,' ').split('.').filter(x=>x.trim()).pop().trim();
const pastN = ((it.draft||'').match(/(했어요|했습니다|였어요|었어요|았어요|셨어요)/g)||[]).length;
if (/(이에요|예요|입니다)$/.test(last) && pastN >= 2)
  WARN(it.id, '본문은 과거형인데 마지막이 현재형 — 「~였어요」인지 확인');
// 🚨08-25 폐기 — «둘째 단락 되받음»을 어휘 겹침으로 재려던 검사는 지웠다. 근거는 바로 아래.
if (/[0-9]+\s*명/.test(it.draft) && !it.web && !(it.sources||[]).length) FAIL(it.id, '숫자를 썼는데 근거 0');
// 🆕08-25 금칙어 — 대표가 「못박자」고 한 것들. 전부 «형식» 검사라 코드가 맞다(의미 검사는 §게이트의 한계 참조)
const BAN = [
  [/짰|짜고|짜는|짜서/,               '「짜다」 금칙 → 구성했어요·만들었어요·이어 붙였어요'],
  [/후기에는|후기에 남|후기를 보면|후기에 적/, '참가자 후기 인용 금칙 → 그 사실을 «우리 문장»으로'],
  [/(자리를|시간을)\s*만들었/,        '「~자리를 만들었습니다」 금칙 → 형식어로 닫아라'],
];
for (const [re, msg] of BAN) if (re.test(it.draft)) FAIL(it.id, msg);
```
- ⛔**「소개된 곳」은 하한을 적용하지 않는다** — 원래 한두 문장짜리 칸이다
- 🚨**대표 확정분을 재발행이 변형하지 않았는지도 코드로 대조한다** — `before.draft !== after.draft` 이면 FAIL

###### 🚨🚨 08-25 — **이 게이트를 「읽고」 시작했는데 「돌리지」 않았다**
대표 지시로 초안 품질을 올리는 실험을 3회차 돌렸다. 1회차에 내가 *「규칙 8개 중 5개가 대표 확정본과 충돌한다」*고
그럴듯하게 보고했는데, **그 8개는 내가 새로 지어낸 규칙이었다.** 위 게이트 코드는 한 번도 안 돌렸고,
2회차 첫 문장 대부분이 `첫 문장이 명사 서술로 닫힘` FAIL에 걸린다. **길이도 정반대로 썼다**(활동을 길게·콜라보를 짧게).
대표: *"어떤 워크숍을 진행했어요, ~열었어요 같이 시작하는 걸 우리 계속 이야기 나눴었는데."* — **이미 합의된 규칙이었다.**

- ⭐**스킬을 읽은 것과 게이트를 돌린 것은 다르다.** 초안을 쓰기 **전에** 위 코드를 실제로 실행한다.
- 🔑**새 규칙이 필요하다고 느끼면 «대표 확정본에 먼저 돌린다». 확정본이 걸리면 그 규칙이 틀린 것이다.**
  실제로 3회차에서 내가 만든 「어미가 요→습니다로 교대하면 안 된다」에 **대표 개정안이 걸렸다**(「요다요다요다요다」).
  확정본 104건 실측으로도 완전 교대는 22%로 정상 범위 — **두 회차 동안 없는 문제를 고치고 있었다.**
- 🪤**없는 문제를 고치는 건 틀린 답을 내는 것보다 알아채기 어렵다 — 성실해 보이기 때문이다**(1팀 08-25).
  ⭐**작업량 자체가 알리바이가 된다**: 확정본 104건 파싱·유사도 측정·채점기 제작이 전부 정당한 행위였는데
  **전부 내가 만든 축 위에 있었다.** 방향이 틀리면 정밀도는 오히려 해롭다 — **틀린 자리에 소수점을 붙여 준다.**
- 🎨같은 병의 다른 사례(1팀·디자인팀 08-25): NEW 칩 색을 새로 판정했는데 볼트 [[디자인-시스템]] §⑩에 **대비 숫자까지 똑같이** 있었다.
  🚨**결과가 맞았다는 사실이 오히려 은폐 장치였다**(틀렸으면 걸렸을 것). 👉**도출한 값이 볼트·스킬에 이미 있는지부터 대조하라 — 같은 값이 나오면 그건 검증이지 발견이 아니다.**

##### 🚨 폐기 기록 — **「둘째 단락 되받음」을 코드로 재려다 틀렸다** (08-25 저녁, 터 건)

대표 규칙(*「2째 단락이 첫째를 보강하면 살리고, 별개 주제면 지워라」*)은 **맞다.**
틀린 건 그걸 **어휘 겹침으로 대신 재려 한 내 코드**였다. 확정본 4건(2단락짜리)에 돌려 보니 —

| 확정본 | 판정 | 통과 근거 |
|---|---|---|
| 캔버스가든 | 통과 | **「있어요」** |
| 전종원 | 통과 | **「일을」** |
| 파랑~ | ⚠️걸림 | — |
| 방혜리 | ⚠️걸림 | — |

**절반이 걸리고, 통과한 둘은 무의미한 말로 통과했다.** 판별력이 0이다.
그런데 파랑~ 님 2단락은 *「제가 목표하는 방향은 독서모임과 비슷합니다」*로 1단락의 프로그램을 **비유로 보강**한다 —
**어휘는 안 겹치는데 의미로는 확실히 보강**이다.

⭐**교훈 = 「보강인가」는 의미 층이라 어휘 겹침으로 대리할 수 없다.** 이건 사람(또는 모델)이 읽어서 판정한다.
🔗같은 병의 세 번째 사례다 — 08-25 아침 `명사 서술 금지`, 낮 `conf 임계값`(§A6), 저녁 이것.
🪤셋 다 **「그럴듯한 대리 지표」를 만들어 의미 판정을 대신하려 한 것**이고, 셋 다 **확정본에 돌리자마자 무너졌다.**
👉**새 검사를 만들기 전에 물어라 — 「이건 형식인가 의미인가」. 의미면 코드로 만들지 마라.**

##### 🚨🚨 게이트의 한계 — **형식 검사로는 품질을 못 잡는다** (08-25 실측)
게이트를 **대표 확정본 104건**과 **방혜리 1차 초안 26건**(제일 나빴던 판)에 나란히 돌려 «판별력»을 쟀다.

| | 확정본 104건 | 1차 초안 26건 |
|---|---|---|
| 옛 게이트(명사 서술 금지 포함) | FAIL **24%** | FAIL **0%** |
| 고친 게이트(홍보문구·시제) | FAIL **1%** | FAIL **0%** |

⭐**두 번 다 「나쁜 초안」을 하나도 못 잡았다.** 옛 게이트는 아예 **거꾸로** 돌았다(좋은 글만 걸렀다).
🪤1차 초안의 진짜 문제는 **62자 · 고유명 없음 · 본인 역할 없음**이었는데, 그걸 검사로 만들 수 없다 —
**캔가 확정본의 사실 밀도가 그 1차 초안과 똑같다**(조각합 1.3). **브랜드마다 기준이 다르기 때문**이다.

👉**그러니 게이트의 일은 「합격 판정」이 아니라 「명백한 위반 거르기」다.**
- 🤖게이트가 잡는 것 = 홍보 문구·금칙어·시제 어긋남·둘째 단락 겉돎 (**있으면 확실히 틀린 것**)
- 👁사람이 봐야 하는 것 = **재료를 충분히 캤나 · 이 항목의 주제가 맞나 · 한 번에 알아듣나**
⛔**「게이트 통과」를 「좋다」로 읽지 마라.** 3️⃣ 알맹이 패스는 여전히 사람 몫이다.

##### 🧊이미 확정된 항목은 **접어서** 보낸다
`frozen: true`인 항목은 카드를 접고 「확정」 배지만 보인다(「문안 펼쳐 보기 ▾」로 펼침).
⭐**스크롤 길이가 곧 검토 비용이다.** 08-24에 50건 중 17건을 접어 대표가 볼 것을 **33건**으로 줄였다.
⛔접은 항목은 **절대 다시 쓰지 않는다.** 대표가 이미 시간을 들여 확정한 것이다.

##### 🔁대표 의견(💬)은 다음 라운드에 **먼저** 적용한다 — 안 본 항목에도
대표가 앞부분을 고치며 남긴 💬는 **뒷부분에 그대로 적용될 규칙**이다. 받자마자 미검토분에 선반영해야 같은 걸 두 번 안 고친다.
08-24에 나온 것 — ⛔거창한 어휘(「희로애락」) / ⛔명사 3개 병렬은 꼬인다(서술로 풀어라) /
⛔마지막 문장이 **사업 진행 방식 설명서**가 되면 안 된다 / ⭕사람은 라벨 말고 **상태**로(「경력 전환자」→「새로운 분야로의 전환을 준비하는 사람」) /
⛔커리큘럼 나열은 **앞의 문제의식과 이어 붙여라**(따로 놓이면 분리돼 보인다).
🚨**단, 규칙을 뽑을 땐 표본을 세라.** 대표가 제목 부제 7개 중 **2개만** 잘랐는데 나는 「부제는 자른다」로 굳힐 뻔했다.
**한두 건을 규칙으로 만드는 것이 3️⃣이 잡으려는 그 오류다 — 규칙 층에서도 똑같이 일어난다.**

#### ✍️ 문안 — 대표 문체 (08-24 방혜리 건에서 역산. 정본은 볼트 [[브랜드-보이스]])
대표: *"뭔가 내가 쓴 것처럼 써야 문장들이 잘 이어지거든."*

- ⭐**고유명은 이름을 밝히고 작은따옴표로 감싼다.** 「여는 모임에」❌ → 「여는 **‘월말 각할모’**에」⭕
- ⭐**압축 명사구·행정용어를 풀어쓴다.** 「정책 사각지대에 있는」❌ → 「정책의 지원이 닿기 어려운」⭕
- ⭐⭐**목적을 담아 명명한다** — 대표 명시: *"~하려 만든, ~도록 만든 자리에요 같은 표현으로"*
  「직접 보여주려고 **열립니다**」❌ → 「직접 만나고 경험할 수 있도록 **만든 자리예요**」⭕
- ⭐**「저는」으로 내 몫을 밝힌다** / 추상→구체(「스스로」→「자신의 지난 시간을」)
- ⭐⭐⭐**닫음은 선언·격언이 아니라 「앞에 쓴 행동을 되받는 매듭」**
  *"'좋은 강의는 내용만으로 완성되지 않는다'가 조금 선언적으로 들려서, 앞의 행동으로 자연스럽게 보여주는 쪽이 더 좋아"*
  → 문형 = **「~한 ○○였어요 / ~한 시간이었습니다」**
- ⭐⭐**마지막 문장엔 마무리 멘트가 붙는다** — *"김밥을 먹어요 이런게 아니라 김밥을 먹으며 마무리해요"*
- **모임은 「누구를 부르나」가 아니라 「거기서 무슨 일이 일어나나」로 정의**
- 문장을 나누고 사이에 분위기 한 줄 / 도치 해소 / 「함께」를 자주 / 구어 축약 해소(「덕에」→「덕분에」)
- 📏~~길이는 칸마다 다르다 — 활동 100~180 / 콜라보 200~250~~ → **08-25 폐기**(확정본 104건 중 콜라보의 76%가 걸렸다). §📏 참조

##### 🚨⭐ 인스타 원문을 **그대로 옮기지 마라** — 대표: *"막 인스타에서 그냥 텍스트 가져온 느낌이 물씬"*
| | |
|---|---|
| 내 초안 | **「‘영업 시작합니다’라는 인사와 함께 상담소 간판을 다시 걸었어요.」** 공동저자 셋이 모여… |
| 대표 | 『든든한 수프상담소』 공동저자 셋이 모여 독자들의 고민을 직접 듣고 이야기하는 **북토크를 진행했어요.** |

「영업 시작했습니다. 고민 받습니다.」는 **팝업 홍보 문구**다. 그걸 문안 첫머리에 박았다.
👉**첫 문장은 인스타 글처럼 「장면」으로 열지 않는다. 「무엇을 한 자리인가」로 연다.**

🪤**그런데 「직접 인용」 자체가 문제인 건 아니다.** 이 지적을 받고 원문과 7자 이상 겹치는 구간을 전수 탐지했더니
**콜라보 7이 제일 많이 걸렸는데, 대표는 그걸 *"너무 좋아 수정할게 없어"*** 라고 했다.
⭐구분선은 **「겹치느냐」가 아니라 「무엇이 겹치느냐」** —
⭕사실·고유명·본인이 붙인 이름·그 자리에서만 나올 구체 장면(「실이 진로·일·퇴사로 몰렸다」)
⛔홍보 카피·모객 문구·이모지 딸린 감탄·원문의 수사

##### 🗂 항목 판정 — 「어느 칸에 넣나」도 대표가 고친다 (08-24)
- ⛔**콜라보 ≠ 거래·의뢰 관계.** 대표: *"피치북스는 발간한거지 콜라보는 아니지 않아..?"* → 출판사·공간 대여는 뺐다
- ⛔**「소개된 곳」 ≠ 수상 이력.** 대표: *"이건 press 라고 보기엔 아닌거 같아. 수상 이력처럼 보여"*
- ⛔**작은 숫자는 굳이 쓰지 않는다.** *"굳이 다섯명이 모였어요 < 이건 어색하고 부족해보여서 삭제했어"*

#### 🪤 아티팩트 운용 — **대표 수정분은 브라우저에만 남는다**
`review-template.html`은 정적 페이지라 **대표가 글상자에서 고친 내용을 우리가 볼 수 없다.**
🚨**재발행하면 그 수정분이 전부 날아간다.** 08-24에 이 사실을 스스로 경고해놓고 **다섯 번을 그냥 올려 유실시켰다.**
- ⭐**규칙: 대표가 「아티팩트 다시 해줘」라고 말하기 전에는 재발행하지 않는다.** 문안은 채팅으로만 드린다.
- 🔁**대표가 「다시 해줘」라고 했을 때의 절차**(1팀 `design-canvas-carousel`의 처방을 이식) —
  **올리기 직전에 매번 최신 저장본을 받아 그 위에 얹는다.** 즉 ⓐ대표에게 `[결정 전체 복사]`를 먼저 받고 →
  ⓑ그 덤프를 로컬 `items.js`에 반영한 뒤 → ⓒ발행한다. **덤프 없이 발행하면 그 사이 수정분이 또 날아간다.**
- 📋덤프(`[결정 전체 복사]`)는 **수정한 항목의 본문만** 싣는다 — 「그대로」 항목의 텍스트는 **우리 로컬 `items.js`가 유일한 원본**이다. 지우지 마라.
- 🚨**대표 문장에 작은따옴표가 있으면 이스케이프하라.** `'커피 한 잔 마실래?'`가 JS 문자열을 끊어 **페이지 전체가 빈 화면**이 될 뻔했다(08-24).
  발행 전 `node -e "new Function(script)"` 로 두 `<script>` 블록을 다 파싱해 본다.


## ⑤ 사진 전체 회수 + 초안 수정

- 대표가 고른 게시물의 사진을 폴더로 회수 — 기법 정본 = [[collab-page-enrich]] §④-B(61장 0실패 실증).

### 📁 사진은 **항상 같은 곳에** 넣는다 (대표 확정 08-24)
```
~/Desktop/collab5/업체사진정리/{브랜드명}/
   활동01_{제목}/ · 콜라보01_{제목}/ · press01_{제목}/
   _브랜드사진 후보/      ← 대표 사진(필수 1장) 후보. 인스타 프로필 hd도 여기
   _기타/{날짜}_{코드}/   ← 어느 항목에도 배정 안 된 게시물
```
파일명 = `{YYYY-MM-DD}_{shortcode}_{NN}.jpg` (하이라이트는 `하이라이트_{이름}_{NN}.jpg`)

🔢🆕**폴더 번호 = 「소개서에 실제로 실린 순서」다** (08-25 터 건). 후보 아티팩트의 순서가 아니다 —
정렬을 「한 일의 시점」 최신순으로 바꾸면 **둘이 어긋나고, 그 어긋남이 그대로 오배치가 된다.**
👉**SQL/폼에 넣은 최종 순서를 먼저 확정하고, 그 순서로 폴더를 만든다.**
⭐**항목 폴더에는 「그 항목의 대표 링크(`url`)인 게시물」의 사진만 넣는다.** 나머지는 `_기타`로.
🪤`sources`를 다 항목 폴더에 쏟으면 **오귀속이 그대로 폴더가 된다** — 08-24에 월간회고클럽 폴더에
각할모·아지트레츠 사진이 들어갈 뻔했다(그 글들이 프로필 줄에 `@monthly_oo_club`을 적어놔서 걸렸다).

### 🗄 수확본은 **백업 폴더에 쌓는다** — 세션이 닫히면 사라진다 (대표 지시 08-24)
```
~/Desktop/collab5-백업/{브랜드}-리서치-{MMDD}/
```
원문 전문 · 홈페이지 원본 HTML · 확정본(`items-final.js`) · 쿼리 · 브리프 · 사진 대장 · 판단 기록 + **README**.
🪤**한 번 잃었다.** 08-22에 받은 인스타 293건이 세션 메모리에만 있다가 날아가 **08-24에 다시 수확**했다.
⭐**요약본만 두면 안 된다** — 소개서의 인원수(15·40·20·30+·15·20·5명)는 전부 홈페이지 원본 HTML에서 왔다. 요약본으로는 대조를 못 한다.

#### 🕸 그리고 `09-관계-스냅샷.json`을 같이 만든다
**제작 시점에 이미 이름이 오간 상대 목록**(본인 글의 `@멘션` + 태그 피드 작성자).
나중에 성사가 잡혔을 때 **「제품이 붙인 것인가」**를 판정할 유일한 근거다 —
파랑~ 님과 방혜리 님은 **소개서(08-21·08-24)보다 먼저인 2026-07-01**부터 서로를 부르고 있었고, 그게 **양쪽 스냅샷으로 증명**된다.
⚠️**비대칭이다** — 있으면 「알던 사이」가 증명되지만 **없다고 「모르는 사이」가 증명되진 않는다**(오프라인 관계).

### 🚨🚨 폴더링한 뒤 **반드시 검증한다** (대표 지시 08-24)
대표: *"저번에 검증했더니 우두두두 변경됐었거든. **잘못 들어가기도, 안 들어가기도** 했었음."*
**받았다고 끝이 아니다.** `verify-photos.mjs` 형태로 **여섯** 가지를 센다 —
① 개수(TSV 행 수 = 실제 파일 수) ② **진짜 JPEG인가**(에러 페이지가 `.jpg`로 저장된다 — 매직바이트 `FF D8 FF`)
③ 5KB 미만(깨짐) ④ **배치**(파일명의 shortcode가 그 폴더에 배정된 코드인가) ⑤ 누락·빈 폴더
⑥ 🆕🚨**수확한 게시물이 «전부» 어딘가에 들어갔나** — 항목 폴더든 `_기타`든.

⑥이 필요한 이유(대표 08-25): *「저번에도 놓친 애들이 좀 있었어, 사진이 너무 적어서 물어보면 또 나오고!」*
🪤같은 날 실제로 걸렸다 — 상한에 걸려 **이어받은 36건의 사진을 안 내려받아서** 두 항목이 **0장**이었다.
⭐**「몇 장 받았나」가 아니라 「받은 게 전부 어디로 갔나」를 세라.** 눈으로는 절대 안 보인다:
```python
allc = {r['c'] for 수확본 전체}          # 게시물 코드 집합
placed = {파일명에서 파싱한 코드}          # 폴더에 실제로 있는 것
assert not (allc - placed), allc - placed   # 이 줄이 ⑥이다
```
🔒**일부러 안 넣은 것이 있으면 폴더에 `_README.txt`로 남겨라** — 08-25에 **회원 사진 477장**
(「PT 횐님」·「회원님 찐 후기」·「비포애프터」 하이라이트)을 남의 몸·얼굴이라 뺐다.
적어두지 않으면 다음 사람이 **「빠뜨렸다」로 오해하고 다시 받는다.**

🪤**검증기 자체를 먼저 의심하라.** 08-24 첫 실행에서 26장이 「잘못 들어감」으로 걸렸는데 **검증기 버그**였다 —
**인스타 shortcode엔 `_`가 들어간다**(`DCyFLXip_jG`·`C_4tyXcpSnT`). `split('_')[1]`로 코드를 자르면 깨진다.
파일명은 `{날짜11자}_{코드}_{NN}` 이므로 **앞 11자·뒤 3자를 떼어내는 방식**으로 파싱할 것.

⏱**서명 URL은 만료된다.** 수집 즉시 내려받아라 — 수집(브라우저)과 다운로드(curl)를 붙여서 한 흐름으로.
- 초안 텍스트 수정·보완 = §④ 폼 조작 정본(네이티브 setter·칩 Enter·임시저장 배너 함정 포함).

### 📐 사진은 **4:3으로 잘린다** — 고르기 전에 알아야 한다 (`PhotoSlider.tsx` `aspect-[4/3]` + `object-cover`)

소개서의 모든 사진(브랜드·활동·콜라보·블록)과 콜라보 카드(`/c`)가 **4:3 고정 틀에 잘라 넣는 방식**이다.
(07-26에 1:1을 시험했다가 대표 판정으로 4:3 복귀 — 코드 주석에 남아 있다.)
- 🚨**스토리·릴스에서 회수한 9:16 세로 사진은 위아래가 크게 잘린다.** 인물 얼굴이나 글자가 위쪽에 있으면 날아간다.
  → 회수 폴더에 세로 사진이 섞였으면 **「가운데에 주인공이 있는 장」을 골라 대표에게 추천**한다.
- ⭐**컨택트 시트를 먼저 만들어 보여줘라**(PIL 6~8열 썸네일 한 장). 대표가 번호로 짚는 게 제일 빠르다 —
  파일명 번호를 시트 라벨과 일치시킬 것. 사진 판단은 대표 몫이고, 우리 몫은 **고르기 쉽게 늘어놓는 것**이다.

### 🚨 문안을 쓰기 전에 — **포인터로 두면 안 읽힌다** (08-15 사고)

문체 정본은 [[collab-page-enrich]] §「문안을 쓸 때의 어투 규칙」이다. **거기를 열어서 읽어라.**
`이 스킬로 들어오면 그 절을 안 거치고 바로 문안을 쓰게 되고, 실제로 그렇게 틀렸다`(콜마페 콜라보 2건, 대표가 두 번 되돌림).
최소한 아래 넷은 **여기서** 확인하고 간다 — 전부 그때 어긴 것들이다:

1. 🚨**길이로 재지 마라 — 문장 수도, 글자 수도.**(08-25 개정. 옛 「100~180자·최대 2문단」은 폐기)
   ⛔「최대 3문장」(08-23 폐기)도 「100~180자」(08-25 폐기)도 **한두 브랜드의 관찰을 굳힌 것**이었고,
   전수로 돌리니 확정본이 걸렸다. **같은 오류를 두 번 냈다.** 자세한 근거는 §④-B §📏.
   · **재료가 적으면 짧게 끝낸다.** 대표: *「차라리 좀 짧되 맞고 필요하고 중요한 말 위주로」*
   · **250자 넘으면** 운영정보·감상이 섞였는지 확인하고 **그것만** 지운다(길다는 이유만으론 안 자른다)
   ⭐**전체 규칙과 대표 퇴고 패턴 11가지 = [[collab-page-enrich]] §문안을 쓸 때의 어투 규칙.** 문안 쓰기 전에 그 절을 연다.
2. **날짜·시각·가격·할인·참여조건은 쓰지 않는다.** 소개서는 계속 걸려 있어서 행사가 끝나면 틀린 정보가 된다.
3. **감상·비유·계절 묘사 금지**(짧은 칸 한정). 그건 `story`의 몫이다.
4. **닫음은 형식어** — `~팝업을/모임을/콜라보를 열었어요·진행했어요`. ⛔`~자리를/시간을 만들었습니다`.
5. 🆕⭐**첫 단락이 메인 주제를 «전부» 담는다. 둘째 단락은 그것을 «보강»할 때만 쓴다.**(대표 08-25)
   대표 원문: *「1번째 단락이 어쨌든 모든 메인 주제를 담는 핵심 단락이자 문장들일 테니깐」*
   · ⭕**살린다** = 둘째가 첫째를 **강조·디테일하게 보강**해서 **첫째가 더 좋아질 때**
     (양천 건 — 1단락 「3주 수업」 → 2단락 「첫 주엔 뭘 하고 마지막엔 뭘 했나」. **같은 대상을 더 자세히**)
   · ⛔**지운다** = 별개 내용으로 **동떨어진 둘째 단락**. 대표: *「쓸모없는 2째 단락은 그냥 삭제하는 게 나음」*
     (두더지 건 — 1단락 「워크숍」 → 2단락 「토우의 역사」. **새 주제를 꺼냈다**)
   🤖1차 판정은 코드로 — **둘째 단락 첫 문장이 첫째 단락의 말을 되받는가**(공유 어휘 0이면 경고).
   🪤**확정본 73%가 1문단이다.** 둘째 단락은 기본값이 아니라 **예외**다.
6. 🆕⭐**마무리는 «어미»가 아니라 «시제»의 문제다.**(08-25, 확정본 104건 전수로 정정)
   대표 지적: ⛔`~수업이에요` → ⭕`~수업이었어요`. 처음엔 이걸 **「명사 서술 금지」로 굳혔다가 폐기**했다 —
   🚨확정본에 **「~워크숍이에요」「~프로그램이에요」「~콜라보예요」가 15건** 있다. **정상적인 정의문**이다.
   ⭐진짜 규칙 = **끝난 일은 과거형, 이어지는 일은 현재형.**
   · 3주짜리 수업을 «진행했어요»라고 열었으면 마무리도 **`~수업이었어요`**
   · 지금도 열고 있는 워크숍이면 **`~워크숍이에요`가 맞다**
   🤖검사도 그렇게 바꿨다 — 본문에 과거형이 2회 이상인데 마지막이 현재형이면 **경고**(FAIL 아님).
7. 🆕⭐**메인 키워드를 먼저 잡고 그것을 축으로 쓴다.**(대표 08-25)
   대표: *「메인 키워드가 수선조형임을 알았다면, 수선 조형을 기준으로 많이 작성되면 좋을 거 같아」*
   🚨08-25 실측 — 내 2·3차 캔가 문안에 **「수선조형」이 한 번도 안 나왔다.** 재료는 다 모으고 **축을 못 잡았다.**
   👉리서치를 끝내면 **「이 브랜드를 한 단어로 하면?」**을 먼저 정한다.
   🚨**단 그 말을 모든 항목에 박으라는 뜻이 아니다.** 캔가 확정본 실측 — 「수선」이 **한 줄 소개·자세히 소개엔 있고,
   활동은 7건 중 2건(제목에 그 말이 들어간 것)뿐, 콜라보는 10건 중 0건**이다.
   ⭐**키워드는 위 두 칸에서 «세우고», 아래 항목들은 그 아래 자연히 놓이면 된다.** 억지로 끼우면 그게 지어낸 문장이 된다.
8. 🆕**곁가지는 버리는 게 아니라 «제자리를 찾아 준다».**(대표 08-25)
   「수선조형 오브제」 항목에서 오마모리 설명이 길어 주제가 흩어졌는데, 대표는 그걸 **지운 게 아니라 「벙개」 항목으로 옮겼다.**
   ⭐**제목에 집중** — 이 항목의 제목이 무엇인지 보고, 거기서 벗어난 재료는 **어느 항목의 것인지** 먼저 묻는다.
9. 🆕🚨**추상적인 원문을 그대로 압축하지 마라 — 「한국말로 이해가 안 간다」가 나온다.**(대표 08-25)
   내가 쓴 *「환경은 수선에서 따라 나오는 결과이지 시작은 아니라는 생각으로 썼어요」*에 대표: **「무슨 말인지 모르겠어. 한글말로 이해가 가지 않아」**
   🪤원문이 *「수선적 측면의 환경은 수선을 통해 나오는 결과론적인 것」*처럼 **추상적일 때 그대로 줄이면 더 어려워진다.**
   ⭐처방 = **그 말이 실제로 가리키는 장면으로 바꿔 쓴다.**
   ⛔`환경은 결과이지 시작이 아니다` → ⭕**`환경을 위해 고쳐 쓰자는 쪽이 아니라, 고쳐 쓰다 보면 환경에도 닿는다는 순서로`**
   🔍자기점검 = **「이 문장을 처음 보는 사람이 한 번에 알아듣나」**. 두 번 읽어야 하면 원문이 추상적인 것이고, 푸는 건 내 몫이다.
10. 🆕🚨**원문에 있는 말이어도 그 항목의 주제가 아니면 「지어낸 말」로 읽힌다.**(대표 08-25)
   벙개 항목에 넣은 *「무엇을 만드는지도 모른 채…」*를 대표가 **「그냥 지어낸 말 같다」**고 했는데 **인스타 원문에 있는 문장**이었다.
   🔗1팀 짝규칙(`collab-brand-brief-gift` §⑤): **한 글 안에 있으면 사실이지만, 다른 항목으로 옮기면 다시 내 편집이 된다.**

11. 🆕⛔**「짜다」는 쓰지 않는다** (대표 08-25 「못박자」)
   ⛔`수업을 짰어요` `프로그램을 짜고` → ⭕**`구성했어요` `만들었어요` `이어 붙였어요`**
   🪤한 소개서 안에서 **반복해서 튀어나온다**(터 건에서 활동·콜라보 양쪽에 나왔다). 게이트에 정규식으로 넣어라.

12. 🆕⛔**자기 자랑이 되는 인용은 옮기지 마라** (대표 08-25 *「으시대는 느낌」*)
   원문에 있는 말이어도 그렇다. ⛔`판매 글에도 "책 팔아서 저에게 떨어지는 금전적 수익은 없습니다"라고 적어 두었습니다`
   → **행동만 남긴다**: `수익을 여성청소년 생리대 지원 사업에 전액 기부했어요.`
   🔗§⑤-2의 「자기 입으로 자기 강점을 선언하면 자랑처럼 읽힌다」와 같은 축이다. **인용은 그 방어막이 못 된다.**

13. 🆕⛔**참가자 후기를 인용하지 마라** (대표 08-25, 터 건에서 **2회** — 활동·콜라보 각 1)
   대표: *「후기 얘기는 별로 안 해도 될 거 같아」*. 후기는 **근거로는 쓰되 문안에는 안 넣는다.**
   ⛔`후기에는 "…"라는 말이 남았습니다` `세 시간이 눈 깜짝할 사이에 지나갔다고 후기에 남겼어요`
   → ⭕**그 후기가 말하는 사실을 우리 문장으로**: `각자의 속도로 책을 읽고 이야기를 나눴습니다`

14. 🆕⛔**둘째 단락에 사적인 고생담을 넣지 마라** (대표 08-25)
   대표: *「2단락 얘기가 너무 사적인 얘기라 본문을 너무 가볍게 만드는 것 같아서」*
   ⛔`모객이 열 명도 차지 않던 시기를 지나…`(첫 행사의 불안) → 본문 무게가 내려간다.
   ⭐**같은 자리를 「왜 열었나」로 채우면 산다** — 실제로 그렇게 바꿔 통과했다(아래 15).

15. 🆕⭐**닫음은 독백이 아니라 「왜 열었는지」로** (대표 08-25, 터 건 **2회**)
   대표: *「역도에 대한 생각 서술이 이렇게 들어가면 좋을 거 같아. 기존에는 너무 좀 독백같아 보였음, 결국 왜 열었는지로 끝냈어」*
   ⛔`…정직함이 좋아서 계속 배워 온 운동이에요.` (내 생각에서 멈춤 = 독백)
   ⭕`…정직함이 좋아서 꾸준히 배워왔고, **그 경험을 나누는 마음으로 함께 클래스를 열었습니다.**`
   ⭐**생각 → 그래서 무엇을 했나**까지 가야 한다. 🔗§⑤-2 「의미로 닫아라」의 구체 문형이다.

16. 🆕🤝**「찾는 콜라보」·「제공 콜라보」는 «우리 계획»이 아니라 «상대가 무엇을 할 수 있나»로 쓴다** (대표 08-25, **2칸 모두 수정**)
   ⛔`〈바디토크〉 두 번째 편을 함께 만들 분을 기다려요`(우리 계획을 공지) → 대표가 **삭제**
   ⭕`여성의 몸과 움직임을 다루는 코치님, 그런 자리가 어울리는 공간과의 콜라보면 좋겠습니다`
   ⭕제공 칸엔 **지역을 밝히고**(`성북구에 운동 공간을 운영하고 있어`), **상대 기준으로**
   (`함께하는 브랜드의 종목이나 관심사에 맞춰 … 서로의 방식을 섞어 새로운 클래스를 만들어볼 수 있어요`)
   🪤이 칸을 읽는 사람은 **파트너 후보**다. 우리 로드맵이 아니라 **자기가 낄 자리**를 찾는다.

17. 🆕⛔**압축 축약(「~터라」·「~덕에」)을 풀어 쓴다** (대표 08-25 직접 고침)
   ⛔`공간이 아쉬웠던 **터라** 이번에는…` → ⭕`공간이 아쉬웠던 **경험이 있어** 이번에는…`
   🔗§⑤ 「압축 명사구·행정용어를 풀어쓴다」의 **어미판**이다. 문어체 축약은 대표 문체가 아니다.

18. 🆕⭐**자랑이 되는 항목은 «검토 때는 넣어서 보이고, 최종은 대표가 정한다»** (08-25 · 표본 1)
   기부·후원처럼 값진 일인데 앞에 두면 자랑처럼 읽히는 항목이 있다.
   🚨**이 규칙은 같은 날 «두 번» 움직였다 — 그래서 아직 규칙이 아니다.**
   | 시점 | 대표의 처리 |
   |---|---|
   | 후보 검토 | *「있으면 좋으나 뭔가 업체의 자랑이라 **일단 넣고** 내가 **맨 마지막 쪽으로 순서를 바꿀게**」* |
   | **최종 발행** | **삭제** (「무턱대고 후원」 — 발행본에 없다) |
   ⭐**확실한 것은 하나뿐 — 우리가 먼저 빼지는 않는다.** 후보엔 올리고, 남길지·뒤로 보낼지·뺄지는 대표가 정한다
   ([[dont-narrow-before-the-owner-sees]]). ⛔「자랑이니 빼자」도, 「맨 뒤로 두면 된다」도 **우리가 정할 일이 아니다.**
   📌**규칙 12(자화자찬 «인용» 금지)와는 다른 층이다** — 12는 «문장»이고 이건 «항목의 거취»다. 12는 확정된 규칙이다.
   🪤이 항목은 **근거가 하이라이트 4장뿐이고 사진도 이체 화면 한 장**이었다. 「자랑이라서」인지 「재료가 얇아서」인지
   갈리지 않았다 — **다음 건에서 재료가 두꺼운 자랑 항목을 만나면 그때 갈린다.**

19. 🆕⭐**「글 쓰는 일」도 활동이다 — 팬이 붙는 층을 빼지 마라** (대표 08-25)
   내가 「빙스레터」(에세이 연재)를 *「활동이라기보다 글쓰기 습관에 가깝다」*며 🟡 뺄 후보로 냈는데
   대표: ***「이건 명백한 활동임! 팬들도 있을거 같고!」***
   🪤**「돈을 받았나·행사인가」로 활동을 재면 이런 층이 통째로 빠진다.**
   ⭐판정축은 §④-B의 그것 그대로 — **「이 브랜드다움이 드러나는가」**. 연재·기록·뉴스레터가 여기 걸린다.

20. 🆕⭐⭐**데이터에 걸리면 «버리기 전에 주장의 층을 낮춰라»** (대표 08-25, 터 브리프에서 확립)
   3️⃣이 잡는 건 **근거가 아예 없는** 문장이다. 그건 지우는 게 맞다.
   그런데 **「부분적으로만 참인」** 문장은 다르다 — 지우면 관찰까지 같이 사라진다.
   | | 문장 | 판정 |
   |---|---|---|
   | 내 1판 | 배우러 **간 자리에서** 사람이 남는다 | ⛔ 5건 중 1건만 참 (인과 주장) |
   | 내 수정안 | 한 번 부른 분을 **다시 부른다** | ⭕ 참이지만 **관찰이 사라짐** |
   | **대표** | **배움 «뒤에»** 사람이 남는다 | ⭕ 전부 참 + 관찰이 남음 |
   ⭐**층의 순서 = 인과(「~때문에」·「~에서 시작해」) → 순서(「~뒤에」) → 공존(「같이 있었어요」).**
   👉①층을 낮춰 보고 ②안 되면 범위를 좁히고(「세 곳 중 한 곳은」) ③그래도 안 되면 버린다.
   ⛔**①②를 건너뛰고 ③으로 가면 문장이 통계가 된다.** 🔗전문 = [[collab-brand-brief-gift]] §⓪

⭐**"매력 포인트가 없다"는 지적을 길이로 갚지 마라.** 남길 것은 **그 콜라보에서만 나올 수 있는 고유 조각**
(토마토 빙수·도자기 짜이잔 같은 사물, "미용실 원장님" 같은 역할)이지 문장 수가 아니다. 판별 문장 =
**"이걸 빼면 이 콜라보가 다른 콜라보처럼 보이나?"** — 표와 전체 규칙은 enrich 스킬 해당 절에 있다.

### ✍️ 문안 — **대표 퇴고 패턴** (정본 = 볼트 [[브랜드-보이스]] §13·§14)

🆕⭐**08-21에 초안 22개와 대표 확정본을 전수 대조해 11가지 패턴을 뽑았다** → [[브랜드-보이스]] §14.
요약: 순서(유래→의미→방식, 운영정보는 뒤) · 압축 명사구 삭제 · 일반명사→고유명사 · **본인이 붙인 이름** ·
의문문은 서술로 · **형식을 명명하며 닫기** · 한 항목 안에서 시제 분리 · 과거 장면→누적 상태 ·
~~두 문단 100~180자~~ → **08-25 폐기**(확정본 73%가 1문단) · 활동 제목엔 행위, 콜라보 제목은 「파트너 — 프로젝트명」.
🪤그리고 **한 회차만 보고 프로그램 전체인 줄 아는 병** — 시즌제는 최신 회차만 보면 반드시 틀린다.

#### (08-18 전종원 작가님) 대표 퇴고에서 반복해 걸린 다섯 가지

한 줄 소개와 자세히 소개를 대표가 각각 **세 번씩 되돌렸다.** 매번 다른 문장을 고쳤는데
**판단은 하나였다 — 내가 지어낸 것을 버리고 브랜드가 이미 가진 것을 세웠다.** 다음엔 처음부터 그렇게 쓴다.

**1. ⭐고유명은 「본인이 붙인 이름」에서 가져온다.**
「주 1회 팀원이 되는」 일을 나는 *일주일에 하루 → 1일 일원*으로 두 번 지어냈고, 대표는 **「렌탈 일원」**으로 바꿨다.
작가님 프로젝트가 「**렌탈** 전종원」이라 그 말이 이미 있었다. 내 조어는 밖에서 붙인 설명이고 이건 **본인 어휘**다.
🔗판정은 예시 오염과 같은 축 — **「그 말의 출처가 입력인가」**. ⭕본인 것이면 1급 재료, ⛔내가 만든 말이면 후보에도 못 낀다.
→ **그래서 ①브리프에 「본인이 붙인 이름」 칸을 뒀다.** 문안 쓸 때 그 칸부터 연다.

**2. 🚨「의미로 닫아라」는 「의미를 지어내라」가 아니다.**
`brandpage-copy-payoff`의 ⭕의미로 닫기를 나는 **새 문장을 창작하라**로 읽고
*"찍는 사람과 쓰는 사람이 나뉘지 않으니 처음 본 장면이 마지막 지면까지 그대로 갑니다"* 를 넣었다 — **대표가 통째로 지웠다.**
그럴듯하지만 **작가님이 한 말이 아니고**, 자기 입으로 자기 강점을 선언하는 꼴이라 자랑처럼 읽힌다.
⭐**대표가 한 일 = 이미 문단에 있던 사실 하나(「책의 뒤편에 함께 만든 이로 이름을 올리며」)를 끝자리로 옮긴 것.**
그 문장은 **사실이면서 동시에 의미**다(=스쳐 가는 외주가 아니라는 뜻). 지어낸 의미는 검증할 수 없지만 이건 책을 펴면 있다.
→ ⭕**착지점을 만들지 말고 「이미 가진 사실 중 의미가 되는 것」을 찾아 끝으로 옮겨라.** 없으면 사실로 담담히 끝내는 게 낫다.

**3. 문장이 놓인 칸이 맞는지 본다 — 옮기면 사는 문장이 있다.**
위 문장은 원래 내가 **팀원(렌탈) 문단** 끝에 뒀는데, 실은 **기록·책 만드는 일의 결말**이었다.
🪤한 문단 안에서만 고치려 들면 안 보인다. **"이 문장은 어느 일의 끝인가"를 물어라.**

**4. 같은 고유명을 세 칸에 반복하지 않는다.**
「렌탈 전종원」이 한 줄 소개·자세히 소개·활동 제목에 **세 번** 나오자 대표가 자세히 소개에서 뺐다
(「렌탈 전종원 프로젝트를 통해」 → 「프로젝트를 통해」). **주 1회·6개월만 남겨도 뭘 말하는지 통한다.**
⭐소개서는 한 화면에서 위아래로 읽힌다 — **칸을 따로 채우지 말고 「방금 위에서 읽은 것」을 전제로 써라.**

**5. 🎭역할 동사가 사실과 맞는지 검증한다.**
내가 쓴 *「소규모 브랜드 동행을 깊이 있게 **이끌어 갑니다**」* 를 대표가 *「깊은 동행을 **이어가기도 합니다**」* 로 고쳤다.
**남의 팀에 팀원으로 들어가는 일인데 「이끈다」고 쓰면 앞뒤가 안 맞는다.**
🔗[[who-authored-the-field]]의 **"우리가 맡은 몫이 뭔가"** 를 동사에도 적용하라 —
주도(이끌다·기획하다)인가 참여(함께하다·맡다·이어가다)인가. 크게 쓰면 좋아 보일 것 같지만 **거짓말이 된다.**

**+ 구조 하나**: 대표는 한 줄 소개의 **「주로 ~ 가끔은 ~」** 대비를 **자세히 소개 두 문단에도 그대로** 심었다
(문단1=주업 기록, 문단2=곁일 팀원). ⭐**한 줄 소개에서 쓴 구조를 자세히 소개가 이어받으면 글이 저절로 정돈된다.**

**+ 길이**: 한 줄 소개 상한은 20~40자인데 대표 최종본은 **54자**였다(코드에 강제 제한 없음).
⚠️**상한을 이유로 구조나 고유 어휘를 깎지 마라** — 대표는 길이보다 *주로/가끔은* 대비와 「렌탈」을 택했다.

## ⑥ 대표 마무리 → 끝 = 📸 스냅샷 B

대표가 회수 폴더에서 사진을 골라 업로드 → 저장. ⑤재검증 보고([[collab-page-enrich]] §⑤ — DNA 영향 여부 포함).

### ✅🚨 **완성 체크는 «칸 목록»으로 — 눈으로 훑지 마라** (08-24 키워드를 통째로 빠뜨림)

2팀이 *「활동 26·콜라보 12·사진 76장으로 완주」*라고 보고했는데 **키워드 칸이 통째로 비어 있었고 대표가 발견했다.**
🪤§②에 *「시작 이야기와 키워드는 저장 후 수정 모드에서」*라고 적혀 있는 걸 읽어놓고 **시작 이야기만 넣었다.**
👉**「확인했다」는 눈으로 훑는 게 아니라 칸 목록을 놓고 하나씩 세는 것이다.**

| 칸 | 어디서 넣나 | 확인 |
|---|---|---|
| 이름 · 한 줄 · 자세히 · 사진 · 주소 · 인스타 · 대표 URL | `/register` 신규 | 화면 |
| **콜라보 유형 칩(공용 1세트·필수)** · 찾는 설명 · 제공 설명 | `/register` 신규 | 화면 |
| **시작 이야기** | 저장 후 `?edit=` | `story` |
| **🚨키워드(최대 10)** | 저장 후 `?edit=` | `cardinality(keywords)` |
| 활동 · 콜라보 · 선택 블록(press 등) | 모달 / `+ 추가` | 개수 |
| 항목별 사진 · 아티클 링크 | 각 카드 | 0장·0링크 항목 찾기 |

### 🗃 **41건을 폼으로 넣지 마라 — 껍데기는 폼, 본문은 SQL** (08-24 실증)
활동 26 + 콜라보 12 + press 3 = **41건**이었다. 폼 카드를 41개 만드는 건 위험하다고 판단해 대표가 열어둔 쿼리 경로를 썼다.
1. **폼**으로 이름·한 줄·자세히·**사진**·주소·인스타·칩 → 저장 → `slug` 발급 (사진 업로드는 폼이어야 한다)
2. **SQL 한 방**으로 `activities` · `collab_history` · `showcases` · `story` 주입 → **대표가 실행**
3. 다시 SQL로 `keywords`

🚨**컬럼 타입이 섞여 있다. 확인하고 써라.**
- `activities` · `collab_history` · `showcases` · `trust` = **jsonb** → `'[...]'::jsonb`
- **`keywords` · `offers` · `seeks` · `target_audience` = `text[]`** → `ARRAY['a','b']::text[]`
- 🪤`activities`가 jsonb인 걸 보고 `keywords`도 그럴 거라 넣었다가 **42804로 튕겼다.** 확인 쿼리도 다르다 — jsonb는 `jsonb_array_length`, text[]는 **`cardinality`**
- ⛔**코드로는 못 알아낸다** — `repo.ts`가 둘을 똑같이 넘긴다(Supabase 클라이언트가 처리). **DB 에러가 유일한 근거**다

🪤**폼 함정 둘**(08-24 실측) —
- **「예: 캔버스가든」 칸이 «둘»이다.** 첫 번째는 **AI 위저드 입력칸** — 거기 넣고 [시작하기]를 누르면 **유료 콜이 돈다.** 두 번째(「브랜드 이름 *」)에 넣어라
- **사진 업로드가 텍스트를 날린다.** 순서를 **사진 먼저 → 텍스트**로. 그리고 `input.files`에 넣는 것만으론 React가 안 받는다 —
  `DataTransfer`로 다시 심고 **`input._valueTracker.setValue('')`** 한 뒤 `change`를 쏴야 붙는다
- **제출 버튼이 뷰포트 밖(고정 푸터)일 수 있다.** `elementFromPoint`가 `null`이면 클릭이 허공을 때리는 것 — JS `.click()`으로

### 🔢 마무리 검증은 **항목별로 세라** — "다 넣었다"는 눈으로 안 보인다 (08-18)

대표가 사진을 다 넣고 *"싹 다 읽어봐"* 했을 때, **콜라보 두 건(뉴웨이즈·콜마페)만 0장**이었다.
폴더엔 각각 14·16장이 있었으니 **빠뜨린 것**이다. 🪤페이지를 위에서 아래로 훑으면 절대 안 보인다 —
사진이 있는 항목이 훨씬 많아서 **"잘 들어갔네"로 읽힌다.** 게다가 콜라보는 **「N건 더 보기」로 접혀** 있어 눈에도 안 띈다.

⭐**항목명 ↔ 장수를 표로 뽑아 0장을 찾아라.** 접힌 것까지 세야 하니 DOM 순서로 훑는다:

```js
// /m/{slug} 에서 — "활동 N"·"콜라보 N" 라벨과 img[alt^="브랜드 사진"]을 DOM 순서로 짝짓는다
const res=[]; let cur=null;
const w=document.createTreeWalker(document.body, NodeFilter.SHOW_ELEMENT);
for(let n=w.currentNode; n; n=w.nextNode()){
  const t = n.childElementCount===0 ? (n.textContent||'').trim() : '';
  if(/^(활동|콜라보)\s*\d+$/.test(t)) res.push(cur={label:t, imgs:0});
  if(n.tagName==='IMG' && /^브랜드 사진/.test(n.alt||'') && cur) cur.imgs++;
}
res.filter(r=>r.imgs===0)   // ← 이게 보고할 목록
```
- **0장이 정상인 항목도 있다**(사진 자체가 없는 콜라보). 회수 폴더와 대조해 **"폴더엔 있는데 0장"**만 보고한다.
- 같은 방식으로 **한 줄 소개·자세히 소개가 최신본인지**도 페이지에서 눈으로 확인한다(SQL 실행 후 캐시가 남을 수 있다).
**스냅샷 B**(최종본)와 A의 격차를 한 줄로 보고한다 — *"사람 손이 얼마나 들었나 = 이 영업을 몇 곳까지 감당하나."*

---

## 기존 스킬과의 관계

- [[collab-brandpage-creation-basic]] **모드 A**(순수 제품 생성)는 **"고객이 혼자 만들면"의 하한 측정용으로만** 남는다.
  ⚠️단 사진 필수(08-05) 때문에 모드 A도 저장하려면 사진 1장이 필요해졌다 — 측정만 하고 저장 안 하는 용도로.
- [[collab-brandpage-creation-basic]] **모드 B는 이 스킬로 대체**된다(모드 B 스스로 "정본은 enrich"라 선언한 상태였다).
- [[collab-page-enrich]]는 **이미 발행된 소개서 단독 보강**용으로 계속 유효 — 이 스킬은 그걸 제작 흐름에 편입한 것.

## 금지·비용 요약

- 유료 콜 = 위저드 1회(합 ~100원). **대표 승인 후에만.** 콜 상한 4(검색1+재시도2+생성1).
- ⛔ 동의 전 브랜드에 전량 수확 금지 / 인스타 로그인·우회 금지 / 사진 자동첨부 재시도 금지 / 대표 픽 전 prod 수정 금지.
- 보고: 브리프(①) → 승인 → 스냅샷 A(③) → 후보 페이지(④) → 재검증 + A/B 격차(⑥).
