---
name: collab-session-wrap
description: 세션 마무리 한 큐 — 옵시디언 볼트 갱신 + 메모리 갱신을 끝까지 마친 뒤 /compact 하도록 넘긴다. "컴팩트할게", "옵시디언·메모리 업데이트해줘", "세션 정리하자", "마무리하자" 요청 시 사용. ⚠️ /compact 자체는 CLI 명령이라 스킬이 대신 못 누른다 — 스킬은 ①②를 끝내고 손잡이만 넘긴다. (2026-08-01 확립)
---

# collab-session-wrap — 세션 마무리

```
① 세션 훑기 → ② 볼트 갱신 → ③ 메모리 갱신 → ④ 보고 + "이제 /compact 하세요"
```

⚠️ **`/compact`는 스킬이 못 누른다.** ①~③을 **끝까지** 해두고 마지막에 대표가 한 번 누른다.
그러니 ④ 보고 없이 끝내지 마라 — 대표는 그 보고를 보고 컴팩트해도 되는지 판단한다.

⛔ **이 작업은 서브에이전트에 위임하지 마라**([[delegate-by-default]]의 명시 예외).
무엇이 결정이고 무엇이 잡담이었는지, 어떤 판정이 뒤집혔는지는 **이 세션의 대화에만** 있다.
서브에이전트는 git log와 파일만 보므로 **결정의 '왜'를 통째로 잃는다.**

---

## ① 세션 훑기

먼저 **무엇을 남길지** 정한다. 코드가 이미 말하는 건 남기지 않는다.

```bash
git log --oneline -30            # 이 세션 커밋
git log -1 --format=%H -- <볼트에 마지막으로 적힌 커밋>   # 어디까지 기록됐나
```

**남길 값이 있는 것** (이 중 해당되는 것만):
- **결정과 그 근거** — 특히 *왜 다른 선택지를 버렸는가*. 이게 없으면 다음 사람이 같은 걸 또 제안한다.
- **뒤집힌 판정** — "확정했다가 다시 보니 아니었다"는 그 자체가 데이터다.
- **사고와 재발 방지** — 무엇이 깨졌고, 순서·전제 중 무엇이 틀렸나.
- **실측으로 정정된 통념** — 문서·메모리에 틀리게 적혀 있던 것.
- **대표가 판단해야 남은 것** — 미결 질문.

**남기지 않는 것**: 코드 구조, 커밋 메시지에 이미 있는 사실, 이 세션에서만 쓰는 임시 정보.

---

## ② 볼트 갱신 (`~/Desktop/collab5-Obsidian/`)

### 어디에 쓰나

| 성격 | 파일 |
|---|---|
| 진행 중인 작업의 상태·QA 라운드 | `1_Projects/<해당 노트>.md` |
| **왜 이렇게 했나 / 무엇을 버렸나** | `2_Areas/의사결정-로그.md` (날짜 절 추가) |
| 화면·기능 정책이 바뀜 | `2_Areas/제품-정책서.md` ⭐[[policy-doc-with-vault-updates]] |
| 디자인 토큰·패턴·타이포 | `2_Areas/디자인-시스템.md` |
| 카피 규칙·말투 | `2_Areas/브랜드-보이스.md` |
| 배포·DB·환경변수·운영 절차 | `2_Areas/인프라-운영.md` |
| 수익·전략 | `2_Areas/BM-전략.md` |
| DB 컬럼 변화 | `3_Resources/tech-APIs/DB-스키마.md` |
| 완료돼 치울 것 | `4_Archive/` 로 이동 + INDEX에서 내림 |

### ⭐ 현재 상태 문서 vs 역사 기록 — 다르게 다룬다

- **현재 상태를 말하는 문서**(제품-정책서·DB-스키마·인프라-운영·스킬) → **내용을 고친다.** 낡은 문장을 남기면 다음 사람이 그걸 믿는다.
- **그때의 결정을 남긴 기록**(의사결정-로그·완료된 `1_Projects` 노트) → **원문을 지우지 말고 주석을 덧댄다**(`~~취소선~~` + "→ MM-DD 변경").
  역사를 덮어쓰면 "왜 그랬더라"를 못 찾는다.

### INDEX.md — 세 곳

1. **프로젝트 표의 해당 줄** — 상태·요약 갱신
2. **📢 알림(hook)** — 다른 팀이 알아야 할 것(액션 불필요). 대표가 "~팀에 알려줘"라 했을 때도 여기
3. **⚠️ 미읽 멘션(mention)** — 상대가 **해야 할 일**. 체크박스로. 끝난 건 `[x]` + 취소선 + 결과 한 줄

---

## ③ 메모리 갱신 (`~/.claude/projects/-Users-youngduck-Desktop-collab5/memory/`)

**먼저 `MEMORY.md`를 읽고 중복부터 확인한다.** 있으면 새로 만들지 말고 **그 파일을 고친다.**

**메모리에 넣을 것** = *다음 세션이 이걸 모르면 헛발질할 것*:
- 상시 지켜야 할 규칙·함정 (`type: feedback`)
- 외부 환경의 실측 사실 (모델 지형, 서드파티 동작 등)
- 대표의 작업 방식 선호
- ⛔**틀린 게 발견되면 지우거나 고친다** — 낡은 메모리는 없느니만 못하다

**볼트 vs 메모리**: 볼트는 *프로젝트의 기록*, 메모리는 *내가 다음에 안 틀리기 위한 것*.
같은 사실이 양쪽에 있어도 되지만, 메모리는 **한 줄로 압축**하고 정본은 볼트를 가리킨다.

파일 하나당 사실 하나 + `MEMORY.md`에 한 줄 포인터.

---

## ④ 보고 + 넘기기

무엇을 어디에 썼는지 **표로** 보고하고, 마지막에:

> 정리 끝났습니다. 이제 `/compact` 하시면 됩니다.

**미결로 남은 것은 반드시 명시**한다 — 대표 판단이 필요한 질문, 확인 못 한 사실.
컴팩트되면 대화는 사라지고 **볼트·메모리에 적힌 것만 남는다.**

---

## 🪤 함정 (전부 실제로 당한 것)

- **중간에 끊기면 반쪽이 남는다.** 2026-07-31에 볼트 갱신이 끊겨 한 노트의 frontmatter만 바뀌고 본문은 옛날 상태로 남았다. → **파일 하나를 시작하면 그 파일은 끝내고 다음으로 간다.** 여러 파일을 동시에 열어놓지 마라.
- **에이전트 보고를 그대로 옮기지 마라.** 값이 큰 주장은 코드·커밋으로 재확인한 뒤 적는다(08-01에 이 방식으로 메모리 오류를 잡았다).
- **한 층을 지우면 남은 층을 다시 재야 한다.** 문서도 같다 — 항목을 폐지하면 그걸 전제로 쓴 다른 문장들이 전부 거짓이 된다. 폐지 시 **전수 검색**(`grep -rn` 볼트+스킬+메모리)으로 어긋난 문장을 찾는다.
- **스킬도 갱신 대상이다.** `.claude/skills/*/SKILL.md`가 옛 정책을 전제로 쓰여 있으면 다음 실행에서 헤맨다.
- **INDEX는 크다**(200줄 근처). 통째로 읽지 말고 `grep -n`으로 해당 줄만 찾아 고친다.

## ⛔ 금지

- `/compact`를 눌렀다고 말하지 마라 — 못 누른다.
- 대표가 결정하지 않은 것을 결정된 것처럼 적지 마라. 미결은 미결로 적는다.
- 볼트에 **비밀번호·API 키·개인 실명**을 적지 마라.
- 다른 팀 세션이 쓴 볼트 문서를 임의로 고치지 마라(그쪽 정본이면 DM으로 알린다).
